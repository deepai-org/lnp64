# LNP64 POSIX and Libc Conformance Matrix

This matrix is the compatibility ledger for the emulator, real Clang/lld path,
and libc shim surface. It should be updated whenever a real package exposes a
missing runtime, libc, ISA, or ABI behavior.

Status values:

- `implemented`: the API lowers to LNP64 instructions or emulator primitives.
- `tested`: the API has a repository test or scripted real-program smoke test.
- `partial`: useful subset exists, but known POSIX/libc behavior is missing.
- `unsupported`: intentionally outside the current v1 compatibility surface.
- `native extension`: LNP64-specific API or instruction surface.

Aggregate gates:

- `toolchain/lnp64_conformance_gates.manifest` is the checked gate-category
  contract for asm demos, C/libc tests, randomized emulator tests,
  adversarial/fault tests, package tests, NetBSD personality gates, planned
  LLVM-built versions, and aggregate hygiene.
- `toolchain/lnp64_toy_compiler_retirement.manifest` keeps the legacy
  assembler path limited to assembly and RTL flat-exec smoke generation,
  records the custom Rust C frontend as removed, and requires C, package,
  userland, and NetBSD coverage to use real Clang/lld/run-elf gates.
- `scripts/run_software_gates.sh` runs the host Rust tests, toolchain contract
  checks, the planned LLVM gate dry-run, demos, userland, NetBSD personality
  smoke/system gates, and the real Clang/lld package gate.
- `scripts/run_all_gates.sh` adds the Dockerized RTL/proof and synthesis/FPGA
  gates plus whitespace checks for full repository hygiene.
- `scripts/run_netbsd_personality_system.sh` is the current layered NetBSD
  personality system gate for Clang/lld-built inputs and incremental
  NetBSD-derived components.

| API or Scope | Status | Evidence | Known Gaps / Compatibility Bugs |
| --- | --- | --- | --- |
| `_start`, `main(argc, argv, envp)`, `environ` | tested | `toolchain/crt0_lnp64.s`, `crt0_startup_stub_matches_crt_contract`, `c_start_symbol_overrides_main_entry`, `c_main_receives_argc_argv_and_envp_from_startup_page`, `c_main_environ_points_at_startup_envp`, libc-test `argv` | Static Clang/lld links use the checked crt0 startup stub; broader hosted runtime packaging and custom `_start` profiles remain under `COMPAT-ABI-001`. |
| `getauxval`, auxv metadata, process entry metadata | tested | `c_getauxval_startup_metadata_surface_runs`, `env_get_copies_process_entry_record_and_faults_bad_buffers`, `env_get_record_copies_are_length_bounded`, `env_get_record_rejects_locked_result_before_buffer_or_errno_side_effects` | Dynamic loader auxv contract is not frozen. Covered by `COMPAT-ABI-002`. |
| `ENV_GET` feature, time, security, scheduler/WCET, topology, and limit discovery | tested | `env_get_reports_public_scalar_metadata`, `env_get_copies_topology_records_and_faults_bad_buffers`, `env_get_rejects_bad_keys_and_never_exposes_random_material`, `env_get_auxv_rejects_result_aliasing_secondary_return_register`, `demos/env_get.s` | Discovery covers reference feature bits, object profiles, security profile bits, classifier limits/features, servicelet verifier limits, scheduler latency constants, topology records, bad-key rejection, no random-material disclosure, and buffer/result prevalidation. Bit assignments and scheduler/WCET constants are reference-emulator v1 values; future hardware profiles may add bits or tighter bounds while preserving existing key semantics. |
| NetBSD/POSIX compatibility lowering and syscall dispatch tables | tested | `src/lowering.rs`, `compatibility_table_names_native_primitives`, `netbsd_system_gate_surfaces_are_registered`, `compatibility_surfaces_have_layer_policy`, `netbsd_syscall_numbers_route_to_compat_surfaces`, `netbsd_syscall_dispatch_is_layered_over_native_lowerings`, `netbsd_system_gate_syscalls_are_registered` | The tables cover current NetBSD system-gate surfaces and an initial NetBSD-current syscall-number subset for supported calls. Full NetBSD syscall coverage and richer per-personality policy remain future work under `COMPAT-PERS-001`. |
| TLS/thread pointer, `errno`, `strerror` | tested | `c_thread_pointer_and_specific_storage_are_per_thread`, `c_errno_location_shim_tracks_hardware_errno`, `c_strerror_returns_static_errno_messages`, `completion_helpers_are_errno_compatibility_boundary`, `completion_helpers_reject_locked_result_before_errno_update`, `mmap_success_clears_errno`, `allocation_success_clears_errno`, `poll_fd_success_clears_errno`, `ready_fd_waiter_completes_result_register_and_errno`, `psABI.md` | Emulator completion helpers are the current compatibility boundary for result/errno updates, with focused success-clear and locked-result prevalidation coverage. Dynamic TLS module layout is not frozen; a broader audit of direct opcode completions remains ongoing. |
| `atexit`, `exit`, `_exit` | tested | `c_atexit_handlers_run_before_main_return`, `c_exit_runs_atexit_but_exit_bypasses_it` | No shared-object destructor model. |
| Environment APIs: `getenv`, `setenv`, `unsetenv`, `clearenv`, `putenv` | tested | `c_environment_surface_stores_and_finds_values`, real-Clang libc-test `env` | Locale/environment inheritance across `exec` needs more real-program coverage; the mutable `environ` table is compact and intentionally small. |
| Basic file descriptors: `open`, `openat`-style lowering, `close`, `read`, `write`, `lseek` | tested | `lowers_file_builtins_to_fd_instructions`, `scripts/run_sbase.sh`, `scripts/run_real_llvm_lnp64_docker.sh` (`sbase` echo/path/cat/wc/head/cmp/cksum/uniq/tail/tee/cp/cut/ls/mkdir/ln/chmod/chown/touch/mv/rm run-elf smokes) | Host-backed compatibility file model; native boot-image and namespace-service integration remain future work. |
| Namespace root/cwd capability semantics | tested | `namespace_root_capability_is_required_for_path_resolution`, `chdir_without_namespace_root_preserves_cwd`, `mkdir_without_namespace_root_does_not_create_host_dir`, `unlink_without_namespace_root_does_not_touch_host_file`, `rename_without_namespace_root_does_not_touch_host_files`, `symlink_without_namespace_root_does_not_create_host_link`, `hard_link_without_namespace_root_does_not_create_host_link`, `stat_without_namespace_root_does_not_write_output_record`, `readlink_without_namespace_root_does_not_write_output_buffer`, `open_fd_dyn_without_namespace_root_does_not_allocate_fdr`, `openat_dyn_without_namespace_root_does_not_allocate_fdr_or_bypass_with_dir_cap`, `open_dir_dyn_without_namespace_root_does_not_allocate_fdr`, `namespace_rejects_empty_paths_instead_of_bypassing_root`, `namespace_root_rejects_lexical_escape`, `namespace_root_rejects_symlink_escape`, `getcwd_path_prevalidates_output_span_before_writing`, `userland/namespace_test_clang.c` | All host-backed path APIs are still compatibility services, but emulator path resolution is rooted in an explicit process namespace root/cwd or directory capability; failures are expected to preserve cwd, host files, output buffers, and FDR allocation state. |
| Positioned and vectored I/O: `pread`, `pwrite`, `readv`, `writev` | tested | `c_readv_writev_surface_uses_dynamic_fdr_io`, emulator fd dispatch tests | Scatter/gather edge cases need real package coverage. |
| Descriptor duplication: `dup`, `dup2`, `fcntl`-style capability duplication | tested | `c_posix_descriptor_dup_surface_runs_on_cap_dup`, `c_fcntl_unsupported_constant_fails_with_einval`, `c_fcntl_accepts_dynamic_command_expressions`, dynamic FDR token tests, real-Clang libc-test `fcntl_basic_bounded`, real-Clang libc-test `fcntl` | `F_GETFD`/`F_SETFD` now drive FDR close-on-exec metadata, and emulator coverage pins `F_DUPFD` lowest-free-slot behavior. Full Linux flag and cross-process lock edge cases are partial. `COMPAT-LIBC-001`. |
| Metadata and filesystem mutation: `stat`, `fstat`, `access`, `chmod`, `chown`, `link`, `symlink`, `mkdir`, `unlink`, `rename`, `utime`/`touch` | partial | libc-test `stat`/`utime`; real-Clang libc-test `access_bounded`; `c_filesystem_mutation_negative_paths_report_errno`; real LLVM metadata libc smoke and sbase object/static-link coverage in `scripts/run_real_llvm_lnp64_docker.sh` | Real Clang now builds and runs upstream libc-test `stat` and `utime`, bounded metadata-backed `access`, plus `stat`/`fstat`/`fcntl`/`futimens`, cwd/dir open, `mkdir`/`rename`/`link`/`symlink`/`readlink`, and `chmod`/`chown` metadata shims through `toolchain/liblnp64_meta_min.c`; sbase `mkdir`, `ln`, `chmod`, `chown`, `touch`, `ls`, `find`, `mv`, and `rm` now run through `run-elf` against a namespace root. Missing-path, duplicate-link, rename, symlink/readlink, and errno paths have focused coverage. `COMPAT-FS-001`. |
| Directory iteration: `opendir`, `readdir`, `rewinddir`, `closedir` | tested | `c_directory_iteration_surface_reads_entries`; real LLVM sbase `ls` and `find` execution in `scripts/run_real_llvm_lnp64_docker.sh` | Concurrent directory mutation semantics are not specified. |
| stdio streams: `fopen`, `freopen`, `fread`, `fwrite`, `fgets`, `fprintf`, `fscanf` scanset subset, `ungetc`, `feof`, `fflush`, `fclose`, `fseek`, `ftell`, `tmpfile`, `setvbuf` | tested | `c_fgets_reads_lines_from_descriptor_stream`, `c_fprintf_writes_formatted_output_to_descriptor_stream`, `c_ungetc_scanset_and_fread_pushback_run`, `c_stdio_*`, `c_tmpfile_returns_read_write_unlinked_stream`, `c_freopen_replaces_descriptor_stream`, real-Clang libc-test `ungetc`/`fgets-eof` | General `scanf` grammar, buffered stream error flags, and wide I/O are partial. `COMPAT-STDIO-001`. |
| Formatting and string/memory helpers: `printf` subset, `snprintf` subset, `str*`, `mem*`, integer division, `ctype` | partial | Lua portability/string tests, `c_standard_ctype_surface_runs`, `indirect_memcpy_and_memset_builtins_have_callable_labels`, real LLVM sbase/jsmn package gate coverage, libc-test `ctype_bounded`/`string`/`string_memcpy_bounded`/`string_memmem`/`string_memmove_bounded`/`string_strchr`/`string_strcspn`/`string_strstr`/`udiv` | Full format grammar, floating formatting, locale collation, and wide chars are partial. `COMPAT-LIBC-002`. |
| Math/libm used by real packages | partial | `c_libm_integer_model_shims_run` | Integer-model shims only; real floating libm remains incomplete. `COMPAT-LIBM-001`. |
| Allocation: `malloc`, `free`, `realloc`, `calloc`, `aligned_alloc`, `posix_memalign` | tested | `c_allocator_libc_surface_uses_native_heap_metadata`, `c_multithreaded_allocator_pressure_runs`, allocator demos, `demos/allocator_native.s`, libc-test `regression/malloc-0` | `malloc(0)` returns non-null unique pointers. Multi-threaded allocator pressure exists; longer randomized mixed-size churn remains under `COMPAT-STRESS-004`. |
| `brk`, `sbrk` | tested | `c_brk_sbrk_compat_surface_uses_native_heap` | Compatibility cursor is modeled over native allocations, not a contiguous Unix heap. |
| `mmap`, `munmap`, `mprotect`, `msync` | tested | `c_mmap_mprotect_and_munmap_surface_runs`, `c_msync_fails_cleanly_until_service_owned_flush_exists`, `mmap_success_clears_errno`, `mmap_rejects_unknown_protection_bits_without_vma_side_effects`, `mmap_rejects_overflow_and_out_of_range_hints_without_vmas`, `mmap_rejects_overlapping_hint_without_mutating_vmas`, `hinted_mmap_does_not_rewind_default_mapping_cursor`, `munmap_rejects_partial_or_interior_ranges_without_unmapping`, `mprotect_rejects_overflow_and_unmapped_ranges`, `anonymous_mmap_remap_zero_fills_old_bytes`, `file_backed_mmap_zero_fills_short_read_tail`, `no_access_mmap_faults_on_load_and_store`, `alloc_ex_creates_and_frees_guard_regions`, `userland/mmap_test_clang.c` | `msync` constants and call surface fail cleanly with `ENOSYS` until service-owned flush exists. The real-Clang NetBSD mmap child covers anonymous map/protect/unmap plus compatibility `errno` after an unmapped `mprotect`. Add Object-Backed Page Transaction Protocol tests. File-backed shared mapping coherence is service/personality policy. `COMPAT-MM-001`. |
| W^X, NX, guard pages, ASLR, `RANDOM` | tested | `wx_mmap_and_mprotect_follow_domain_policy`, `nx_and_guard_instruction_fetches_fault`, `heap_and_anonymous_mmap_use_aslr_layout`, `random_scalar_and_buffer_are_deterministic`, `randomized_mmap_mprotect_and_guard_stress_preserves_permissions`, `demos/secure_jit.s`, `demos/guarded_heap_overflow.s` | Seeded VMA/guard stress exists; broader file/shared mapping fuzzing remains under `COMPAT-STRESS-003`. |
| `getentropy`, `getrandom`, `arc4random`, `arc4random_buf` | tested | `c_entropy_surface_lowers_to_random_instruction`, `random_obeys_domain_entropy_quota` | Cryptographic entropy source is intentionally not modeled; deterministic emulator RNG is for architecture behavior. |
| `pthread_create`, `pthread_join`, `pthread_detach`, cancellation controls, `pthread_self`, thread-specific storage | tested | `c_thread_pointer_and_specific_storage_are_per_thread`, `c_pthread_join_reports_lifecycle_errors`, `c_pthread_detach_reports_lifecycle_states`, `c_pthread_cancellation_controls_fail_cleanly`, `c_pthread_tsd_destructors_run_on_thread_return`, `thread_detach_live_thread_rejects_join_and_discards_completion`, `thread_detach_completed_thread_consumes_join_status`, `thread_detach_rejects_locked_result_before_state_change`, libc-test `pthread_tsd`, pthread tests | Join self/missing/double-join errors, detach live/completed/locked-result lifecycle states, and deterministic cancellation-control `ENOSYS` behavior are covered. Real cancellation delivery, robust mutexes, and scheduler fairness are partial. `COMPAT-PTHREAD-001`. |
| Mutexes, condvars, rwlocks, `pthread_once`, semaphores | tested | `c_pthread_mutex_condvar_surface_runs_on_futex_primitives`, `c_semaphore_and_once_surface_runs_on_futex_primitives`, `c_posix_semaphore_timedwait_and_returning_pthread_start_run`, `c_rwlock_surface_runs_on_futex_primitives`, libc-test `sem_init` | Timed semaphore waits are covered; broader timed condvar/rwlock and process-shared behavior remain partial. `COMPAT-PTHREAD-002`. |
| C11 atomics and futex-backed waits | tested | `c_c11_atomic_surface_runs_on_lock_cmpxchg`, `real LLVM LNP64 run-elf C11 atomic execution passed`, `futex_wake_removes_empty_waiter_entry`, `repeated_parking_does_not_duplicate_wait_queue_entries` | Add TSO/seq-cst/futex acquire-release litmus tests under `COMPAT-MEM-001`. |
| `poll`, `select`, `epoll_create1`, `epoll_ctl`, `epoll_wait` | tested | `c_select_fdset_surface_lowers_to_readiness_probe_and_runs`, `c_select_blocks_with_dynamic_await_and_runs`, `c_poll_*`, `c_epoll_surface_lowers_to_native_readiness_probe_and_runs`, `c_poll_race_timing_cases_run`, `c_epoll_wait_race_timing_cases_run`, `c_poll_closed_shared_descriptor_reports_pollnval`, `stale_fd_waiter_rejects_reused_event_source_generation`, `stale_dynamic_fd_waiter_reports_error_result`, `fd_waiter_reports_error_when_poll_right_revoked_before_wake`, `ready_fd_waiter_completes_result_register_and_errno`, `multi_source_fd_waiters_wake_only_ready_sources`, `fd_waiters_survive_repeated_pending_churn_until_ready` | Basic race timing, descriptor reuse/stale-generation, poll-right revocation, ready-result completion, shared-fd close, multi-source waiter, repeated pending-churn coverage, and real-Clang `EPOLL_CTL_MOD` data replacement exist; broader randomized scheduler interleavings remain under `COMPAT-STRESS-005`. |
| `kqueue`, `kevent` | partial | real-Clang poll/select/epoll/kqueue libc smoke; `userland/poll_test_clang.c` | NetBSD-shaped constants and C call surfaces compile, EVFILT_READ and EVFILT_WRITE readiness over queue capabilities run through the same `AWAIT`/waitable path as poll/select/epoll in the NetBSD poll child, EVFILT_USER/NOTE_TRIGGER runs as a process-local trigger source, EV_DELETE removes a registered readiness source, EV_DISABLE suppresses delivery without removing the registration, EV_ENABLE resumes delivery, EV_ONESHOT removes a source after first delivery, EV_RECEIPT returns an `EV_ERROR` success receipt with zero data, unsupported filter registrations fail closed without an event buffer or report `EV_ERROR`/`EINVAL` when an event buffer is supplied, and `close(kq)` invalidates the process-local kqueue before a new empty kqueue can be opened. Broader kernel-backed filters remain partial. |
| `eventfd`, `timerfd_create`, `timerfd_settime`, `timerfd_gettime` | tested | `c_eventfd_surface_uses_counter_object_profile`, `c_timerfd_surface_uses_object_timer_profile`, `event_counter_write_wakes_reader_waiting_for_nonzero_value`, `timer_expiration_wakes_reader_waiting_for_tick`, `demos/await_timer.s` | Event-counter and timerfd waiter wakeups have focused emulator coverage. Full Linux flag and clock-id behavior is partial. |
| `clock_gettime`, `gettimeofday`, `time`, `nanosleep`, `usleep`, `alarm` | tested | `c_time_surface_uses_realtime_pcrs_and_sleep`, `c_usleep_and_alarm_surface_runs`, libc-test `clock_gettime` | Time precision is emulator-coarse. |
| `fork`, `exec*`, `wait`, `waitpid`, `getpid`, `getppid` | tested | `c_posix_process_and_signal_mask_surface_runs`, `c_wait_and_getppid_surface_runs_after_fork`, `c_wait_reports_default_signal_termination`, `c_exec_family_lowers_to_native_exec`, `fork_clone_inherits_fdr_generation_rights_and_metadata`, `fork_clone_inherits_signal_state_and_clears_pending_events`, `fork_clone_does_not_copy_in_flight_ipc_or_waiters`, `fork_clone_copies_vma_heap_metadata_and_isolates_memory`, `fork_clone_stresses_vma_heap_mutation_independence`, `process_exec_preserves_fdr_capabilities_and_namespace_state`, `exec_closes_fdrs_marked_close_on_exec_before_replacement`, `waitpid_live_child_parks_until_child_exit_event`, `thread_join_waiter_wakes_and_consumes_exit_status`, `repeated_parking_does_not_duplicate_wait_queue_entries`, emulator clone/exec tests | `fork` is constrained `CLONE profile=posix_fork`; focused coverage now pins fork-time FDR inheritance, copied signal masks/dispositions with cleared child pending signals, no copied in-flight IPC inbox entries or waiter ownership, copied VMA/heap metadata with independent post-fork memory writes, larger forked VMA and heap mutation independence, child/thread wait wakeups, no duplicate wait-queue entries, FDR capability/generation, close-on-exec metadata, namespace, and errno preservation across emulator-native `exec`. Software binary-loader work is tracked as `COMPAT-BIN-001`. |
| Frozen signal subset: `sigaction`, `signal`, `sigprocmask`, `raise`, `kill`, `alarm`, child-exit `SIGCHLD`, default/ignore/handler dispositions, fatal default wait status, signal frames, `SIGRET`, arithmetic-fault `SIGFPE` | tested | `c_sigaction_accepts_posix_action_struct`, `c_signal_default_and_ignore_dispositions_run`, `c_wait_reports_default_signal_termination`, `c_hardware_fault_signal_surface_runs_through_handler`, `signal_controls_reject_invalid_numbers_without_mutation`, `invalid_signal_events_do_not_enter_pending_state`, `kill_reports_missing_target_and_full_event_queue`, `process_event_queue_rejects_overflow_without_replacing_pending_events`, `child_exit_signal_respects_parent_event_queue_limit`, `signal_delivery_uses_native_event_queue_before_abi_frame`, `masked_signal_remains_pending_until_unmasked`, `hardware_fault_signal_bypasses_compatibility_signal_mask`, `ignored_signal_and_default_sigchld_are_consumed_without_frame`, `default_fatal_signal_exits_through_native_event_path`, `divide_by_zero_queues_sigfpe_fault_event`, `div_rejects_locked_result_before_fault_event`, `signal_delivery_defers_nested_frames_until_sigret`, `sigret_rejects_stale_signal_frame_generation_without_restore`, `signal_frame_stack_area_is_non_executable`, `userland/signal_gate_test_clang.c`, `userland/signal_fault_test_clang.c`, `scripts/run_netbsd_personality_system.sh` | Mask/pending, alarm, child-exit handler, event-queue overflow, nested-delivery deferral, `SIGRET` signal-frame generation rejection, fatal default signal status, invalid-number rejection, and divide-by-zero fault paths are covered. Add broader synchronous fault classes, trusted user-visible `SIGRET` tokens, interruptible operation `EINTR`/typed-interrupt behavior, and unsupported realtime/restart/altstack fail-or-emulate tests. Full realtime queueing, legacy restart quirks, and OS-specific delivery corners remain compatibility policy. `COMPAT-SIGNAL-001`. |
| Sockets over endpoint capabilities: `socket`, `bind`, `listen`, `connect`, `accept`, `getsockname`, `getsockopt`, `setsockopt`, `send`, `recv` | tested | `c_socket_surface_lowers_to_endpoint_object_controls_and_runs`, `c_socket_options_fail_closed_for_unknown_controls`, `socket_endpoint_object_controls_enforce_capability_rights`, `socket_endpoint_rejects_invalid_addresses_without_state_change`, `socket_accept_prevalidates_destination_before_taking_pending_stream`, `socket_accept_prevalidates_output_slot_before_taking_pending_stream`, `socket_endpoint_create_rejects_unsupported_profiles_without_installing_fd`, `socket_endpoint_state_transitions_reject_stale_capability_tokens`, `socket_getsockopt_prevalidates_value_before_len_update`, `socket_option_controls_reject_unsupported_options_before_buffers`, `socket_getsockname_rejects_short_buffer_without_writes`, `socket_getsockname_prevalidates_output_before_len_update`, `userland/socket_loopback_test_clang.c`, and `demos/netcat.c`/`demos/httpd.c` self-tests in `scripts/run_real_llvm_lnp64_docker.sh` | Endpoint capability shape is native; TCP/UDP semantics are service-owned. Unsupported socket options fail closed with `EINVAL`; the real-Clang socket loopback child covers option `errno`, bind/listen, connect/accept, poll readiness, and send/recv through endpoint-backed libc shims. Native endpoint controls check capability rights, stale endpoint generations, unsupported profiles, invalid addresses, and output-buffer preconditions before committing state. Nonblocking sockets, descriptor passing, datagram endpoints, and real network-driver service integration are partial. `COMPAT-SOCK-001`. |
| Optional dynamic loading and subprocess streams: `dlopen`, `dlsym`, `dlclose`, `popen`, `pclose` | unsupported | `c_optional_dynamic_loading_and_popen_fail_cleanly` | Intentionally fail cleanly until binary/dynamic-loader work exists. |
| Locale, wide chars, iconv, regex | partial | sbase text utilities exercise simple byte-string behavior | Full locale/wide/regex conformance is not implemented. `COMPAT-LIBC-003`. |
| LNP64 `__lnp_*` shim layer | native extension | `intrinsic_manifest_matches_target_manifest`, `intrinsic_lowering_manifest_matches_real_llvm_surface`, `intrinsic_header_matches_intrinsic_manifest`, `userland/gate_trace_test_clang.c`, `psABI.md` | Private native shim names cover open/pull/push/mmap/await/call/gate-return/domain/object/capability primitives, including create convenience shims over typed control records. Public extension header/package boundary is not frozen. |
| Resource Domain APIs | native extension | `c_domain_lifecycle_surface_runs_on_domain_ctl`, `c_domain_child_surface_runs_on_domain_ctl`, `c_domain_limit_failure_runs`, `domain_ctl_manages_nested_resource_domains`, `domain_limits_are_enforced_by_ordinary_operations`, `domain_capability_scope_blocks_ordinary_ops`, `domain_ctl_rejects_unknown_opcode_without_side_effects`, `domain_security_policy_delegation_is_monotonic`, `domain_create_prevalidates_output_record_before_mutation`, `domain_security_rejects_invalid_boolean_selectors_without_mutation`, `domain_query_prevalidates_full_output_record_before_writing`, `domain_configure_rejects_unauthorized_masks_without_mutation`, `domain_security_numeric_policy_delegation_is_monotonic`, `inactive_current_domain_rejects_sensitive_operations`, `domain_usage_rolls_up_and_release_is_live`, `domain_property_invariants_cover_edge_cases`, `userland/domain_nested_test_clang.c`, `userland/domain_budget_test_clang.c`, `userland/gate_trace_test_clang.c`, `randomized_domain_lifecycle_stress_rejects_stale_handles` | VM/container/cgroup/sandbox profiles lower to the same child-domain primitive. Focused tests cover nested lifecycle, budgets, delegated capability/security masks, fail-before-mutation validation, inactive-domain rejection, usage roll-up/release, domain-tree invariants, and real-Clang domain/call-gate/domain-budget execution. Broader concurrent attached-process interleavings remain under `COMPAT-STRESS-002`. |
| Capability APIs: send, receive, duplicate, narrow, seal, revoke, generation/lineage checks | native extension | `c_capability_transfer_surface_runs_on_native_cap_ops`, `cap_dup_can_only_narrow_rights`, `cap_dup_rejects_unknown_flags_without_installing_destination`, `sealed_capability_can_be_used_but_not_duplicated_or_narrowed`, `cap_revoke_invalidates_descendant_capabilities`, `cap_send_requires_transfer_right`, `cap_send_recv_transfers_narrowed_capability`, `cap_send_wakes_reader_waiting_for_capability_payload`, `cap_recv_wakes_writer_waiting_for_capability_queue_space`, `cap_send_move_closes_source_after_queueing_capability`, `cap_recv_cannot_expand_transferred_rights`, `cap_revoke_invalidates_received_capability`, `cap_dup_rejects_locked_result_before_install_or_errno_side_effects`, `cap_revoke_rejects_locked_result_before_revocation_or_errno_side_effects`, `cap_send_rejects_locked_result_before_queue_or_errno_side_effects`, `cap_recv_rejects_locked_result_before_dequeue_or_errno_side_effects`, `demos/capability_transfer.s`, `demos/stale_fd_token.s`, `userland/fd_passing_test_clang.c`, `randomized_capability_delegation_stress_preserves_authority` | Seeded delegation stress exists; focused tests cover rights narrowing, sealing, generation/revoke invalidation, queue transfer rights, waiter wakeups, move-on-send, fail-before-install, no rights expansion on receive, and real-Clang duplicate/send/receive/revoke stale-capability execution. Add lineage epoch propagation across FDR caches, VMAs, events, call gates, DMA, and page-fill continuations. Multi-process/channel interleavings remain under `COMPAT-STRESS-001`. |
| Object APIs: queue/counter/memory object, pipe lowering, message receive | native extension | `c_object_creation_surface_runs_on_object_ctl`, `c_pipe_lowers_to_object_queue_and_runs`, `c_message_receive_lowers_to_await_pull_and_runs`, `object_create_rejects_cross_kind_profiles_without_installing_fd`, `object_ctl_rejects_invalid_records_and_missing_authority`, `object_ctl_create_prevalidates_result_slots_before_installing_fd`, `object_ctl_rejects_locked_result_register_before_create_side_effects`, `object_ctl_eventfd_rejects_unknown_flags_without_installing_fd`, `object_ctl_requested_fd_replacement_releases_old_file_locks`, `memory_object_creation_rejects_zero_length_without_replacing_fd`, `memory_object_creation_rejects_oversized_without_replacing_fd`, `memory_object_creation_counts_backing_storage_in_domain_usage`, `memory_object_write_rejects_growth_past_maximum_size`, `memory_object_write_growth_obeys_domain_budget`, `pipe_writer_rejects_full_byte_queue_and_reports_not_writable`, `pipe_read_wakes_writer_waiting_for_byte_queue_space`, `event_counter_write_wakes_reader_waiting_for_nonzero_value`, `push_rejects_locked_result_register_before_queue_write`, `demos/object_profiles.s` | Focused tests cover typed object creation, cross-kind profile rejection, result-slot prevalidation, locked-result fail-before-side-effect, eventfd flags, requested-fd replacement lock cleanup, memory-object size/budget accounting, pipe queue pressure, and event-counter/pipe wakeups. Multi-producer/multi-consumer race coverage remains partial. |
| Generic classifier table objects and queue routing | native extension | `classifier_routes_ipc_record_by_service_id_and_wakes_queue`, `classifier_routes_packets_by_port_subnet_and_hash`, `classifier_routes_generic_event_records_with_inline_fields`, `classifier_supports_mark_and_count_actions_without_queue_authority`, `classifier_fallback_malformed_and_drop_counters_are_reported`, `classifier_ipv6_zero_payload_length_needs_software_without_routing`, `classifier_ipv4_zero_total_length_needs_software_without_routing`, `classifier_oversized_packet_records_need_software_without_routing`, `classifier_rejects_oversized_routed_records_before_queueing`, `classifier_rejects_full_destination_queue_without_success_counters`, `classifier_rejects_unauthorized_stale_and_revoked_routes`, `classifier_cap_source_and_queue_rights_are_enforced`, `classifier_table_capability_is_generation_checked_and_revocable`, `classifier_table_creation_is_bounded`, `classifier_table_create_failures_do_not_install_requested_fd`, `classifier_table_rejects_invalid_rule_descriptors`, `classifier_rejects_invalid_profile_and_foreign_domain_envelopes`, `classifier_control_records_reject_bad_pointers_handles_and_rights`, `userland/classifier_test_clang.c` | Emulator v1 is a bounded fixed-rule table over record envelopes with exact/masked/range/hash matches and mark/count/drop/route/needs-software actions. Packet parsing is deliberately shallow; unsupported, fragmented, zero-length, malformed, and oversized packet shapes fall back to software or fail before routing. The real-Clang child covers classifier table creation, authorized queue routing, event wakeup, drop/fallback, and counters. |
| Bounded servicelet programs | native extension | `servicelet_program_creation_verifies_bounds`, `servicelet_verifier_accepts_advertised_static_loop_flag`, `servicelet_program_owner_must_be_current_domain_or_descendant`, `servicelet_program_verifier_rejects_bad_envelopes`, `servicelet_verifier_rejections_do_not_install_requested_fd`, `env_get_reports_public_scalar_metadata`, `env_get_copies_topology_records_and_faults_bad_buffers`, `design.md`, `hardware_design.md`, `formal_theorems.md`, `verification_plan.md` | Emulator v1 supports `servicelet_program` creation through `OBJECT_CTL` with a bounded verifier envelope, instruction/cycle/record/action limits, allowed ISA subset checks, owner-domain generation checks, fail-before-install behavior, and `ENV_GET` discovery of verifier limits. Actual servicelet attachment/execution, authorized action records, queue/gate attachment authority, and revocation/generation checks on attachments remain future work. |
| Network servicelet integration | specified | `design.md`, `hardware_design.md`, `verification_plan.md` | Add tests for attaching servicelets to `net_interface`, `packet_queue`, endpoint, and listener objects; cover RX steering, TX admission, accept filtering, priority/mark propagation, telemetry redaction, `needs_software` fallback, destination authority, and stale/revoked attachment rejection. |
| Realtime latency classes and bounded miss behavior | partial | `env_get_reports_public_scalar_metadata`, `env_get_copies_topology_records_and_faults_bad_buffers`, `design.md`, `hardware_design.md`, `formal_theorems.md`, `verification_plan.md` | `ENV_GET` now reports the reference latency class count, Class A/B/C/D bounds, wakeup insertion bound, preemption bound, active-window size, and scheduler topology feature record. Add tests for per-op Class A/B/C/D classification, bounded cold/missing/full/spilled outcomes, `EINPROGRESS` completion tokens, explicit parks, and no hidden DDR/service/path/page-fill work before instruction retire. |
| Shared fabric arbitration and reservations | specified | `hardware_design.md`, `formal_theorems.md`, `verification_plan.md` | Add RTL/emulator models for metadata/event/DMA/memory/servicelet-lane arbitration, Resource Domain admission, pressure events, best-effort throttling, and proof/test evidence that admitted realtime work cannot be blocked indefinitely by best-effort traffic. |
| Typed control envelope: `GET_META`, `SET_META`, `OBJECT_CTL`, `DOMAIN_CTL`, `NS_CTL` | partial | `object_ctl_rejects_invalid_records_and_missing_authority`, `object_ctl_create_prevalidates_result_slots_before_installing_fd`, `object_ctl_rejects_locked_result_register_before_create_side_effects`, `domain_ctl_manages_nested_resource_domains`, `domain_ctl_rejects_unknown_opcode_without_side_effects`, `ns_ctl_resolve_requires_namespace_root_capability`, `ns_ctl_resolve_uses_directory_capability`, `ns_ctl_resolve_nofollow_final_preserves_in_root_symlink_path`, `ns_ctl_resolve_failures_do_not_write_output_buffer` | Emulator v1 has typed record/version/op validation for `OBJECT_CTL`, `DOMAIN_CTL`, and `NS_CTL`, plus missing-rights, namespace-root authority, bounded output, locked-result, and fail-before-install coverage. `GET_META`/`SET_META`, scalar-count/capability-argument tables, returned-capability slots, vendor/device profile authority, copied-vs-pinned buffer semantics, richer `EFAULT`/`EOVERFLOW`/`EBUSY`/`ECANCELED` outcomes, revoke during control, service crash before/after commit, and returned-capability verification remain open. |
| Native service transaction model | specified | `design.md`, `hardware_design.md`, `formal_theorems.md` | Add tests for bounded request records, service generation mismatch, service crash before commit, service crash after commit, returned-capability proposals as data, Capability Engine install rejection, queue backpressure wait/`EAGAIN`/`EOVERFLOW`, revocation during pending service work, pinned-buffer cancellation, and no ambient raw pointer/interrupt/DMA capability exposure. `COMPAT-SERVICE-001`. |
| Call gates: sync, async, handoff | native extension | `c_sync_call_gate_runs`, `call_cap_sync_returns_across_domain_gate`, `call_cap_async_and_handoff_modes_execute_minimally`, `demos/object_profiles.s`, `demos/call_gate_modes.s`, `psABI.md` | Full cross-domain aggregate argument ABI is not frozen. |
| DMA APIs and DMA buffers | native extension | `dma_ctl_copy_and_fill_use_vma_permissions`, `dma_ctl_rejects_guard_unmapped_and_disallowed_domain`, `dma_ctl_uses_dma_buffer_capability_scope`, `dma_ctl_rejects_stale_and_revoked_dma_buffers`, `dma_ctl_v1_completes_before_revoke_and_blocks_later_submits`, `demos/dma_copy.s`, `demos/revoked_dma_buffer.s`, `demos/guarded_heap_overflow.s` | Emulator v1 is synchronous copy/fill only, with no pending descriptor queue. Async DMA completion and quiesce/cancel policy remain future hardware work. |
| Reserved raw port, raw syscall, and microcode hooks | tested | `unprivileged_microcode_load_queues_fault_event_without_port_access`, `raw_port_hooks_require_io_domain_capability`, `inb_rejects_locked_result_before_fault_or_port_access`, `malformed_microcode_load_preserves_existing_port_state`, `c_raw_syscall_function_fails_with_enosys` | Raw `INB`/`OUTB`/microcode hooks are reserved debug/driver paths, not ambient device authority, and C `syscall(...)` fails with `ENOSYS` instead of bypassing compatibility dispatch tables. Unprivileged use queues structured hardware-fault events or fails domain capability checks without touching port state. Real drivers should use delegated `pcie_bar`, `dma_buffer`, and `irq_event` capabilities; fuller PCIe/IOMMU delegation is tracked outside this reserved-hook v1 smoke. |
| Static ELF objects, static PIE, relocations, exec-plan loader boundary | partial | `object_format.md`, `psABI.md`, `toolchain/lnp64_run_elf.manifest`, `toolchain/lnp64_loader_security.manifest`, `exec_rejection_preserves_old_image_before_commit`, `exec_without_namespace_root_preserves_old_image_before_commit`, `exec_unmapped_argv_preserves_old_image_before_commit`, `exec_unmapped_envp_preserves_old_image_before_commit`, `exec_unterminated_envp_preserves_old_image_before_commit`, `exec_oversized_entry_metadata_preserves_old_image_before_commit`, `exec_oversized_envp_metadata_preserves_old_image_before_commit`, `scripts/run_netbsd_personality_system.sh` | Static v1 software-loader ELF loading, checked relocations, loader-produced architectural exec plans, and real Clang/lld `run-elf` execution are covered for the current static subset. This remains partial because broader dynamic-linking policy, archive/library behavior, external VMA source acquisition policy, and richer descriptor/relocation fuzzing are tracked under `COMPAT-BIN-001`; hardware still validates only the resulting exec-plan descriptor and preserves the old image on pre-commit rejection, including namespace-authority failure, malformed target parsing, unmapped argv/envp entry-vector pointers, unterminated envp vectors, oversized startup metadata, malformed RELA section diagnostics, bad entry-PC executable-VMA coverage, reserved mapping flags, measurement authority, and prepared VMA source-authority mismatch before process replacement. |
| Dynamic linker and shared-library runtime | unsupported | `object_format.md`, `psABI.md`, `c_optional_dynamic_loading_and_popen_fail_cleanly` | Intentionally fail cleanly in v1. Future dynamic loading is a software personality/loader profile, not a hardware feature. |
| Bootable userland image: `/sbin/init`, shell-like runner, filesystem image, `/dev` namespace | tested | `userland/init_clang.c`, `userland/lnpsh_clang.c`, `userland/ucat_clang.c`, `userland/spawn_task_clang.c`, `scripts/run_userland.sh`, `scripts/run_real_llvm_package_gate.sh`; `userland/netbsd_init_clang.c`, `userland/netbsd_sh_clang.c`, `userland/{elf_exec_test,loader_target,thread_test,namespace_test,poll_test,fs_service_test,mmap_test,fd_passing_test,gate_trace_test,timer_test,socket_loopback_test,signal_gate_test,signal_fault_test,domain_nested_test,domain_budget_test}_clang.c`, `scripts/run_netbsd_personality_system.sh` | The default userland gate now runs Clang/lld static ELFs through `run-elf`, covering init, shell-like reads, ucat, namespace-rooted file access, and native spawn-entry child execution. The NetBSD personality system gate boots a Clang/lld-linked root through `run-elf --namespace-root`, adds a scripted shell transcript, ELF exec, a generated fixed-record service-owned filesystem image, multiple executed compiled C tests, nested Resource Domain composition, per-child Resource Domain isolation/PID balance, and stale FDR rejection. Native boot manifest plus service-owned namespace/filesystem integration remains under `COMPAT-USERLAND-001`. |

## Real Program Targets

These targets are the current compatibility gates. A target only moves to
`passing` when it builds and runs through a checked-in script without package-
specific compiler/runtime special casing.

| Target | Status | Evidence | Compatibility Bugs |
| --- | --- | --- | --- |
| sbase subset | partial / passing current real-Clang smokes | `third_party/sbase/*`, `scripts/run_sbase.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | The package entrypoint now routes to the real LLVM gate. Current real-Clang execution covers echo, basename/dirname, cat, wc, head, cmp, cksum, uniq, tail, tee, cp, cut, tr, sort, fixed-string `grep -F`, no-regex `sed -n p`, mkdir, ln, chmod, chown, touch, ls, find, mv, and rm; `yes` is Clang/lld-linked and exec-plan validated but not run because the command is intentionally unbounded without a pipeline/kill harness. |
| jsmn | passing | `third_party/jsmn/example/simple.c`, `third_party/jsmn/test/tests.c`, `scripts/run_jsmn.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | The package entrypoint now routes to the real LLVM gate, which compiles and runs a generated no-stdio `jsmn.h` package smoke through `run-elf`. |
| inih | passing | `third_party/inih/ini.c`, `third_party/inih/smoke.c`, `scripts/run_inih.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | The package entrypoint now routes to the real LLVM gate, which compiles and runs a generated string-only `ini_parse_string` smoke through `run-elf` at `-O0`. Broader allocator/custom-reader configurations and optimized inih codegen remain open. |
| zlib upstream | partial / passing checksums | `third_party/zlib/adler32.c`, `third_party/zlib/crc32.c`, `third_party/zlib/crc32.h`, `third_party/zlib/zutil.c`, `third_party/zlib/smoke.c`, `scripts/run_zlib.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | The package entrypoint now routes to the real LLVM gate, which compiles upstream `adler32.c` and `crc32.c` and runs a generated no-stdio checksum smoke through `run-elf` at `-O0`. Full deflate/inflate remain `COMPAT-PKG-002`. |
| natsort | passing | `third_party/natsort/strnatcmp.c`, `third_party/natsort/smoke.c`, `scripts/run_natsort.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | The package entrypoint now routes to the real LLVM gate, which compiles upstream `strnatcmp.c` and runs a generated non-stdio natsort package smoke through `run-elf`. |
| cwalk | passing | `third_party/cwalk/include/cwalk.h`, `third_party/cwalk/src/cwalk.c`, `third_party/cwalk/smoke.c`, `scripts/run_cwalk.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | Unix and Windows style selection, normalize, join, join-multiple, absolute/relative generation, intersection, basename/dirname, segment iteration, and extension replacement pass. The real LLVM gate now compiles upstream `cwalk.c` and runs a generated no-stdio path-normalization smoke through `run-elf` at `-O0`. Broader edge-case enumeration remains under `COMPAT-PKG-007`. |
| small HTTP server | partial / run-elf self-test | `demos/httpd.c`, `scripts/run_real_llvm_lnp64_docker.sh`, `scripts/run_real_llvm_package_gate.sh` | Real Clang object/static-link coverage plus namespace-rooted self-test execution exists. Broader unattended loopback, nonblocking, and network-service semantics remain tracked by `COMPAT-SOCK-001`. |
| netcat-like socket demo | partial / run-elf self-test | `demos/netcat.c`, `scripts/run_real_llvm_lnp64_docker.sh`, `scripts/run_real_llvm_package_gate.sh` | Real Clang object/static-link coverage plus in-process self-test execution exists. Broader unattended loopback, nonblocking, and descriptor passing remain tracked by `COMPAT-SOCK-001`. |
| sqlite-lite demo | passing | `demos/sqlite_lite.c`, `scripts/run_real_llvm_lnp64_docker.sh` | This is not upstream SQLite and currently uses anonymous bootstrap mmap rather than fd-backed database mmap; full SQLite remains `COMPAT-PKG-003`. |
| minimal userland image | passing | `userland/init_clang.c`, `userland/lnpsh_clang.c`, `userland/ucat_clang.c`, `userland/spawn_task_clang.c`, `scripts/run_userland.sh`, `scripts/run_real_llvm_package_gate.sh` | Coverage is Clang/lld/run-elf. Native boot manifest plus service-owned namespace/filesystem integration remains `COMPAT-USERLAND-001`. |
| lnp64-netbsd-personality system | passing | `scripts/run_netbsd_personality_system.sh`, `scripts/run_real_llvm_package_gate.sh`, `src/lowering.rs`, `userland/netbsd_init_clang.c`, `userland/netbsd_sh_clang.c`, Clang-built userland tests under `userland/*_clang.c` | The gate runs a Clang/lld-linked root and child ELF set through `run-elf --namespace-root`. It includes typed compatibility lowering and syscall-dispatch tables, nested Resource Domain composition, ELF exec, and a fixed-record service-owned filesystem image with path walking, mutation, metadata, and flush/barrier checks; a full NetBSD-derived filesystem service, full software EXEC-plan loader, and complete BSD syscall/personality dispatch remain open work. |
| Lua upstream | failing / not checked in | Lua-targeted compiler tests exist; `toolchain/lnp64_real_program_ladder.manifest` records the minimal `lua -e print_1_plus_2` first rung; no checked-in full Lua package gate exists yet. | Remove Lua-specific normalizer pressure by fixing generic C semantics. `COMPAT-PKG-001`. |
| SQLite upstream | not started | `toolchain/lnp64_real_program_ladder.manifest` records the in-memory then file-backed SQLite rung; no checked-in upstream SQLite target exists yet. | Add package gate. `COMPAT-PKG-003`. |
| Redis upstream | not started | `toolchain/lnp64_real_program_ladder.manifest` tracks Redis as a configured static build, single-client smoke, and persistence/fork acceptance gate after NetBSD/POSIX personality closure and tiny network daemons. | Add Redis only after Lua, SQLite, NetBSD/POSIX personality, and network-daemon gates expose the generic runtime gaps. `COMPAT-PKG-008`. |
| libpng upstream | not started | No checked-in libpng target | Add package gate after zlib. `COMPAT-PKG-004`. |
| musl libc-test subset | partial / passing focused checks | `third_party/libc-test/functional/{access_bounded.c,argv.c,basename.c,clock_gettime.c,ctype_bounded.c,dirname.c,env.c,fcntl.c,fcntl_basic_bounded.c,fdopen.c,pthread_tsd.c,qsort_bounded.c,random.c,search_insque.c,search_lsearch.c,sem_init.c,stat.c,string.c,string_memcpy_bounded.c,string_memmem.c,string_memmove_bounded.c,string_strchr.c,string_strcspn.c,string_strstr.c,strtol.c,udiv.c,ungetc.c,utime.c}`, `third_party/libc-test/regression/{fgets-eof.c,malloc-0.c}`, `scripts/run_libc_test.sh`, `scripts/run_real_llvm_lnp64_docker.sh` | `access_bounded`, `argv`, `basename`, `clock_gettime`, `stat`, `utime`, `ctype_bounded`, `dirname`, `env`, `fcntl`, `fcntl_basic_bounded`, `fdopen`, `fgets-eof`, `malloc-0`, `pthread_tsd`, `qsort_bounded`, `random`, `search_insque`, `search_lsearch`, `sem_init`, `string`, `string_memcpy_bounded`, `string_memmem`, `string_memmove_bounded`, `string_strchr`, `string_strcspn`, `string_strstr`, `strtol`, `udiv`, and `ungetc` have real Clang/lld/run-elf coverage through `scripts/run_real_llvm_lnp64_docker.sh` and `scripts/run_libc_test.sh --backend llvm --loader exec-plan`. `COMPAT-PKG-005` tracks remaining libc-test breadth and runtime gaps. |

## Open Compatibility Bugs

| Bug | Requirement | Current State | Next Concrete Step |
| --- | --- | --- | --- |
| `COMPAT-ABI-001` | psABI/crt packaging | `psABI.md` records the current emulator ABI, including native clone-profile spellings and compatibility mappings; `toolchain/lnp64_psabi.manifest`, `toolchain/lnp64_crt_startup.manifest`, `toolchain/crt0_lnp64.s`, `toolchain/lnp64_sysroot.manifest`, `scripts/package_lnp64_sysroot.sh`, `psabi_manifest_records_current_calling_convention_contract`, `llvm_target_manifest_records_required_backend_contract`, `crt_startup_manifest_records_process_entry_contract`, `crt0_startup_stub_matches_crt_contract`, and `sysroot_manifest_records_packaged_crt_layout` pin the backend-facing psABI, clone-profile, startup minimums, packaged crt/header/libc-shim sysroot layout, and a sysroot-backed static-link/run-elf smoke. Static Clang/lld gates assemble and link the checked crt0 stub. | Broaden custom `_start` coverage and freeze hosted startup metadata beyond the current static profile. |
| `COMPAT-ABI-002` | auxv/static-loader metadata contract | `psABI.md` records current auxv behavior; `c_getauxval_startup_metadata_surface_runs`, `env_get_copies_process_entry_record_and_faults_bad_buffers`, `env_get_record_copies_are_length_bounded`, and `env_get_auxv_rejects_result_aliasing_secondary_return_register` cover the current `ENV_GET`/`getauxval` metadata path. | Freeze auxv key numbers and static loader metadata. Dynamic-loader expectations belong to a future software profile. |
| `COMPAT-LLVM-001` | LLVM LNP64 backend | `toolchain_roadmap.md` defines the target skeleton, register/psABI, instruction selection, MC encoding, relocation, inline asm, intrinsic, debug/unwind, clone-profile, and crt milestones. `llvm/lib/Target/LNP64`, `clang/lib/Basic/Targets/LNP64.*`, `clang/lib/Driver/ToolChains/Arch/LNP64.cpp`, `toolchain/lnp64_contracts.manifest`, `toolchain/lnp64_target.manifest`, `toolchain/lnp64_registers.manifest`, `toolchain/lnp64_psabi.manifest`, `toolchain/lnp64_relocations.manifest`, `toolchain/lnp64_mc_encoding.manifest`, `toolchain/lnp64_isel.manifest`, `toolchain/lnp64_intrinsics.manifest`, `toolchain/lnp64_debug_unwind.manifest`, `toolchain/lnp64_inline_asm.manifest`, and `toolchain/lnp64_crt_startup.manifest` plus the `*_manifest_records_*` tests pin the initial backend-facing contract, including `CLONE` profiles. The real LLVM Docker gate now builds and runs object, link, libc, package, and NetBSD/userland smokes. | Broaden backend ABI coverage beyond the current static Clang/lld/run-elf smokes: aggregate ABI corners, large frame-index offsets, varargs, dynamic linking policy, optimized codegen, and fuller upstream package coverage remain open. |
| `COMPAT-RUNELF-001` | Loader-to-emulator ELF execution | `toolchain/lnp64_run_elf.manifest` records tested loader/descriptor/commit stages, committed-image text fetch/decode, and real Clang/lld run-elf execution for demos, libc tests, packages, userland fixtures, and NetBSD personality children. The stdout/exit compatibility row remains partial until the production libc/runtime path replaces the smoke-only shim. `run_elf_manifest_records_execution_boundary` checks that this boundary stays visible in the target manifest, gate manifest, roadmap, loader security contract, and conformance table. | Replace the smoke-only libc shim with Clang-built crt/libc runtime support and prove the same stdout/exit path through the real runtime. |
| `COMPAT-CLANG-001` | Clang, libc, and private `__lnp_*` shim path | `toolchain_roadmap.md` defines the Clang driver/libc layering and freezes raw native operations behind libc/libpthread/libsys/private shims. `toolchain/lnp64_intrinsics.manifest`, `toolchain/lnp64_inline_asm.manifest`, `intrinsic_manifest_matches_target_manifest`, and `inline_asm_manifest_records_backend_constraints` pin the private shim/intrinsic and inline-asm constraint surface. The real LLVM gate now compiles, links, and runs direct `__lnp_push` and `_exit` smokes through `run-elf`; broader libc/package gates remain open. | Expand the Clang-built crt/libc shim beyond the current smoke objects and migrate more libc surfaces onto real Clang/lld gates. |
| `COMPAT-LD-001` | lld/static linking path | `object_format.md` defines ELF64/static PIE policy and `toolchain_roadmap.md` defines lld relocation/linking milestones. `lld/ELF/Arch/LNP64.cpp`, `toolchain/lnp64_relocations.manifest`, `toolchain/lnp64_exec_plan.manifest`, `relocation_manifest_matches_object_format_and_target_manifest`, and `exec_plan_manifest_matches_loader_boundary_contract` pin the initial relocation and loader exec-plan contracts. Static lld links are exercised by the real LLVM Docker gate and then executed through `run-elf`. | Expand relocation coverage, archive/library search behavior, linker diagnostics, and eventual dynamic-linker policy beyond the current static executable profile. |
| `COMPAT-NETBSD-LLVM-001` | NetBSD-derived code built by real toolchain | `userland/netbsd_personality_clang_smoke.c` now builds with real Clang/lld and runs through `run-elf`, covering NetBSD personality init/shell markers plus fd, mmap, pthread, poll-probe, signal, and socket shim paths. `userland/netbsd_init_clang.c`, `userland/netbsd_sh_clang.c`, `userland/loader_target_clang.c`, `userland/elf_exec_test_clang.c`, `userland/thread_test_clang.c`, `userland/poll_test_clang.c`, `userland/signal_gate_test_clang.c`, `userland/signal_fault_test_clang.c`, `userland/timer_test_clang.c`, `userland/mmap_test_clang.c`, `userland/fd_passing_test_clang.c`, `userland/namespace_test_clang.c`, `userland/fs_service_test_clang.c`, `userland/classifier_test_clang.c`, `userland/socket_loopback_test_clang.c`, `userland/gate_trace_test_clang.c`, `userland/domain_nested_test_clang.c`, and `userland/domain_budget_test_clang.c` are the first Clang/lld/run-elf replacements for NetBSD personality system programs, covering init, shell, loader target, ELF exec, pthread, non-fork queue readiness, non-fork signal-gate delivery, hardware fault signal, timerfd/alarm, mmap/VMA errno, fd/capability passing, namespace/cwd/path escape behavior, LNPFS2 image mutation/recovery, classifier route/drop/fallback counters and queue wakeup, socket endpoint loopback, Resource Domain/call-gate, nested-domain, and domain-budget slices. `toolchain_roadmap.md` defines the transition to broader Clang/lld-built libc/libpthread/libm pieces, userland commands, and rump services. | Replace remaining fixed-record exec-plan fixtures with loader-produced exec plans and broaden fork/blocking and SIGCHLD child-flow coverage through real ELF children. |
| `COMPAT-BIN-001` | Binary/object format | `object_format.md` defines static v1 software-loader ELF, relocations, mapping permissions, ASLR, static-only dynamic-linking policy, startup descriptors, and the hardware exec-plan boundary. `toolchain/lnp64_run_elf.manifest` and `toolchain/lnp64_loader_security.manifest` record that the static software loader now parses ELF, applies the checked relocation subset, materializes VMAs, builds architectural exec-plan descriptors, and drives `run-elf`. `exec_rejection_preserves_old_image_before_commit`, `exec_without_namespace_root_preserves_old_image_before_commit`, `exec_unmapped_argv_preserves_old_image_before_commit`, `exec_unmapped_envp_preserves_old_image_before_commit`, `exec_unterminated_envp_preserves_old_image_before_commit`, `exec_oversized_entry_metadata_preserves_old_image_before_commit`, `exec_oversized_envp_metadata_preserves_old_image_before_commit`, `emulator_rejects_exec_descriptor_count_and_length_shape_fuzz`, `emulator_rejects_prepared_exec_vma_source_authority_mismatch_before_commit`, and the loader-security manifest tests cover malformed targets, missing namespace authority, bad argv/envp entry-vector pointers, unterminated envp vectors, oversized startup metadata, descriptor count/length fuzzing, malformed RELA section diagnostics, W^X/NX/provenance rejection, entry-PC executable-VMA validation, reserved mapping flags, measurement authority, prepared VMA source-authority mismatch, FDR grant validation, and generation/lineage mismatch before process replacement. | Broaden the static loader beyond the current tested subset: add external VMA source acquisition policy checks, additional descriptor-shape fuzzing, additional relocation/linker diagnostics, archive/library search behavior, and dynamic-linking policy tests. |
| `COMPAT-USERLAND-001` | Minimal userland image | Clang/lld static ELFs for init, lnpsh, ucat, and native spawn-task pass `scripts/run_userland.sh` through the real LLVM package gate. `scripts/run_netbsd_personality_system.sh` now boots a larger NetBSD-style personality root with `run-elf --namespace-root` and runs the requested shell-style transcript plus compiled namespace, thread, poll/select/epoll, filesystem-service, mmap, fd-passing, call-gate, timer, socket, signal, signal-fault, nested-domain, and domain-budget tests with per-child Resource Domain isolation. | Add native boot manifest/service-image format and boot command instead of host-directory setup. |
| `COMPAT-PKG-001` | Upstream Lua | Lua compatibility is covered by targeted compiler tests only; `toolchain/lnp64_real_program_ladder.manifest` records minimal Lua as the first real-program rung. | Add a reproducible upstream Lua package script and convert failures into generic compiler/runtime bugs. |
| `COMPAT-PKG-002` | zlib | Upstream v1.3.1 Adler-32 and CRC-32 checksum paths build and run through the real LLVM gate without stdio; `scripts/run_zlib.sh` delegates to that gate. | Expand to deflate/inflate once struct-heavy stream state and broader compression runtime paths are ready. |
| `COMPAT-PKG-003` | Upstream SQLite | Only sqlite-lite demo exists; `toolchain/lnp64_real_program_ladder.manifest` tracks the next upstream path as in-memory SQLite before file-backed persistence. | Add upstream amalgamation smoke once C/runtime gaps are known. |
| `COMPAT-PKG-004` | libpng | Not started. | Add after zlib passes. |
| `COMPAT-PKG-005` | musl libc-test subset | Upstream libc-test `argv`, `basename`, `clock_gettime`, `fcntl`, `stat`, `utime`, `ctype_bounded`, `dirname`, `env`, `fgets-eof`, `malloc-0`, `qsort_bounded`, `random`, `search_insque`, `search_lsearch`, `string`, `string_memcpy_bounded`, `string_memmem`, `string_memmove_bounded`, `string_strchr`, `string_strcspn`, `string_strstr`, `strtol`, `udiv`, and `ungetc`, plus bounded `access_bounded` and `fcntl_basic_bounded`, now build and run through real Clang/lld/run-elf. Probed blockers include `basename`/`dirname` null/all-slash behavior now covered by real-Clang `basename` and `dirname`, high-byte C string escapes now-fixed by byte-exact string emission, file-scope static byte arrays now covered by real-Clang `string_strchr`, file-scope two-dimensional byte arrays and `lfind`/`lsearch` fixed-width search semantics now covered by real-Clang `search_lsearch`, word-pointer parameter indexing and POSIX `random`/`srandom`/`initstate`/`setstate` state semantics now covered by real-Clang `random`, intrusive queue `insque`/`remque` and inferred queue-node sizing now covered by real-Clang `search_insque`, descriptor-backed `fdopen` stream reuse now covered by real-Clang `fdopen`, mutable `environ` now covered by real-Clang `env`, argv/startup path and `%s` snprintf formatting now covered by real-Clang `argv`, thread-specific storage destructors and `pthread_key_t` aliases now covered by real-Clang `pthread_tsd`, ASCII ctype predicate/case-map semantics now covered by `ctype_bounded`, realtime PCR clock reads now covered by real-Clang `clock_gettime`, metadata-backed `access` success/missing-path/invalid-mode behavior now covered by real-Clang `access_bounded`, unsigned division/remainder edge cases now covered by `udiv`, upstream record-lock/fork fcntl behavior now covered by real-Clang `fcntl`, `memmem` byte-search semantics now covered by real-Clang `string_memmem`, and `strchr`, `strcspn`, and `strstr` scan semantics now covered by real-Clang `string_strchr`, `string_strcspn`, and `string_strstr`, bounded direct/callable `memcpy`/`memset` semantics now covered by real-Clang `string_memcpy_bounded`, bounded direct/callable `memmove` overlap semantics now covered by real-Clang `string_memmove_bounded`, bounded `strncpy`/`strncat`, `strtok`, and `strlcat` return semantics now covered by real-Clang `string`, base/sign `strto*` parsing, `ERANGE`/`EINVAL`, and octal C integer literals now covered by real-Clang `strtol`, unnamed semaphore init/wait/post, try/timed wait errno behavior, and pthread start routines that return normally now covered by real-Clang `sem_init`, non-null unique zero-size allocations now covered by real-Clang `malloc-0`, bounded qsort comparator word-cast loads, pointer-to-pointer parameter indexing, global-array `sizeof`, qsort string/int/char/uint64 paths now covered by real-Clang `qsort_bounded`, EOF-preserving `fgets` over `fmemopen` now covered by real-Clang `fgets-eof`, descriptor-backed `ungetc` pushback/scanset mismatch/fread consumption now covered by real-Clang `ungetc`, descriptor flag behavior through real-Clang `fcntl_basic_bounded`, stat metadata, and timestamp updates now covered. Full upstream `string_memcpy`/`string_memset` exhaustive alignment loops and full upstream qsort's 1024-element cases remain too slow for the default gate; broader scanf/regex/wide/dynamic-linking tests remain open. | Keep migrating libc-test breadth to real Clang/lld/run-elf; next practical targets are broader scanf/regex/wide/dynamic-linking coverage. |
| `COMPAT-PKG-006` | inih parse-string aggregate path | File parser and `ini_parse_string` smoke paths pass with upstream `inih` r58. The gate fixed generic typedef-aggregate alias layout and function-scoped aggregate-local metadata. | Expand with heap-buffer/realloc/custom-reader configurations if those `inih` build modes become targets. |
| `COMPAT-PKG-007` | cwalk path library | Upstream cwalk commit `e98d23f` has a checked smoke gate for Unix and Windows style selection, absolute/relative predicates, basename/dirname output parameters, segment iteration and replacement, extension detection/replacement corner cases, normalization, path joining, absolute and relative path generation, intersection detection, truncation lengths, empty/root-only paths, repeated separators, mixed separator normalization, drive-relative Windows roots, UNC root variants, relative backtracking, and in-place output overlap. The gate fixed generic C/frontend/runtime gaps for `do ... while` `continue` semantics, word-slot indexing through member pointer arrays, and overlap-safe callable `memmove`. | Broaden further only by importing more of upstream's own test matrix or adding randomized path-case generation. |
| `COMPAT-PKG-008` | Upstream Redis | Not started; `toolchain/lnp64_real_program_ladder.manifest` keeps Redis behind Lua, SQLite, NetBSD/POSIX personality closure, and tiny network daemons so compatibility fixes stay generic. | Add a configured static Redis build gate, then single-client `PING`/`SET`/`GET`/`DEL`, then persistence/fork coverage. |
| `COMPAT-FS-001` | Filesystem/path conformance | Focused emulator/libc tests cover common metadata and mutation paths; real LLVM currently compiles/links the sbase command set and executes the file-read/stdio/path subset including `head`/`cmp`/`cksum`/`uniq`/`tail`/`tee`/`cp`/`cut`/`tr`/`sort`/fixed-string `grep -F`/no-regex `sed -n p` plus namespace-rooted `mkdir`, `ln`, `chmod`, `chown`, `find`, `mv`, and `rm`. `c_filesystem_mutation_negative_paths_report_errno` covers missing-path errno, duplicate hard-link rejection, rename success/failure, unlink failure, and symlink/readlink target preservation. | Add real-Clang execution coverage for more sbase filesystem commands, plus broader symlink traversal, permissions, `stat` field, and rename/link cross-type corner cases. |
| `COMPAT-STDIO-001` | stdio conformance | Common descriptor-backed streams pass; `ungetc` one-byte pushback, `feof` false-before-EOF, `fscanf` scanset mismatch preservation, and `fread` consumption of ungot bytes are covered. | Add real EOF/error flag transitions, general `scanf`, buffering, append, and mixed read/write tests. |
| `COMPAT-LIBC-001` | Full `fcntl` command surface | Descriptor duplication, dynamic command dispatch for the supported subset, close-on-exec descriptor flags, advisory locks, and unsupported-command `EINVAL` behavior are tested. | Add more Linux flag and lock edge cases. |
| `COMPAT-LIBC-002` | Formatting/string/locale | Byte-string subset passes real demos. | Expand printf/scanf, locale, and collation tests. |
| `COMPAT-LIBC-003` | Wide char/regex/iconv | Mostly unsupported. | Decide v1 boundary and add fail-cleanly tests. |
| `COMPAT-LIBM-001` | Floating libm | Integer-model shims exist. | Add real double representation or document static integer-only v1 limit. |
| `COMPAT-MM-001` | VMA/Page Engine state machine and object-backed mmap/fill transactions | Anonymous mapping security, permission narrowing, no-access faults, guard regions, zero-fill behavior, short file-backed tail zero-fill, seeded mmap/mprotect/guard stress, DMA/VMA prevalidation, and deterministic unsupported `msync` behavior are covered by `wx_mmap_and_mprotect_follow_domain_policy`, `mprotect_ignores_overflowing_vma_bounds`, `no_access_mmap_faults_on_load_and_store`, `alloc_ex_creates_and_frees_guard_regions`, `anonymous_mmap_remap_zero_fills_old_bytes`, `file_backed_mmap_zero_fills_short_read_tail`, `randomized_mmap_mprotect_and_guard_stress_preserves_permissions`, `dma_ctl_copy_and_fill_use_vma_permissions`, `dma_ctl_rejects_guard_unmapped_and_disallowed_domain`, and `c_msync_fails_cleanly_until_service_owned_flush_exists`. | Add page-state transition tests, commit/abort point tests, deterministic race-priority tests, page request/reply shape tests, object-fill success/failure, zero/shared-page replies, retry/later behavior, VMA/object generation mismatch rejection, lineage mismatch rejection, executable provenance checks, DMA pin/revoke interactions beyond prevalidation, TLB/I-cache invalidation ordering, pending-fill cancellation on munmap/mprotect/revoke/truncate/signal/domain teardown, dirty-range enumeration, real service-owned `msync`, and shared/file mapping smoke tests. |
| `COMPAT-MEM-001` | Memory visibility contract | Basic atomics/futex behavior exists; `demos/memory_order.s` covers `FENCE`, `ISYNC`, and successful/failed `LOCK_CMPXCHG` smoke behavior. | Add litmus tests for TSO store ordering, store-buffer forwarding, seq-cst `LOCK_CMPXCHG`, futex acquire/release wait/wake, `FENCE` ordering for DMA/engine completions, write-combining fence-before-doorbell, `device_ordered` MMIO ordering, and TLB/I-cache invalidation before resumed execution. |
| `COMPAT-PTHREAD-001` | Full pthread lifecycle | Core create/join/detach/TLS works. `c_pthread_join_reports_lifecycle_errors` covers self-join, missing thread, and double-join errors; `c_pthread_detach_reports_lifecycle_states` and focused emulator tests cover live detach, detached join rejection, completed-thread status discard, missing-thread errors, and locked-result fail-before-state-change. `c_pthread_cancellation_controls_fail_cleanly` pins unsupported cancellation controls as pthread-style `ENOSYS` returns without old-state mutation. | Add real cancellation delivery plus detach/join race stress. |
| `COMPAT-PTHREAD-002` | Timed/process-shared sync | Futex-backed primitives work. | Add timed waits and unsupported process-shared behavior tests. |
| `COMPAT-SIGNAL-001` | Frozen signal subset | Default/ignore/action/mask subset works; the NetBSD system gate covers masked `SIGINT`, alarm delivery, child-exit `SIGCHLD`, and divide-by-zero `SIGFPE` through ABI handlers returning with `SIGRET`. Focused emulator tests cover native event-queue delivery before ABI frame construction, event queue overflow, invalid signal rejection, fault-signal mask bypass, nested-frame deferral until `SIGRET`, stale `SIGRET` signal-frame generation rejection, fatal default exit status, ignored signal/default `SIGCHLD` consumption, and locked-result prevalidation before fault event emission. `c_wait_reports_default_signal_termination` covers fatal default signal wait status through `WIFSIGNALED`/`WTERMSIG`. | Add process-vs-thread pending delivery, deterministic process-directed target selection, broader synchronous fault-to-signal mapping, trusted user-visible `SIGRET` tokens, interruptible operation `EINTR`/typed-interrupt behavior, and unsupported realtime/restart/altstack fail-or-emulate tests. |
| `COMPAT-SOCK-001` | Socket endpoint compatibility | TCP-like local endpoint subset works over endpoint objects. `c_socket_options_fail_closed_for_unknown_controls` covers supported no-op socket options and `EINVAL` for unknown levels/options. Emulator endpoint tests cover capability-right checks, stale token rejection across bind/listen state transitions, unsupported-profile create rejection before fd install, invalid address fail-before-state-change, accept destination/output prevalidation, unsupported option prevalidation before buffer reads, short getsockname buffer rejection without writes, and getsockname/getsockopt output-buffer prevalidation. `demos/netcat.c` and `demos/httpd.c` now have real Clang/lld/run-elf self-test coverage. | Add typed endpoint-contract tests for packet queue record boundaries, datagram message boundaries, stream ordered-byte/backpressure behavior, listener accepted-capability inheritance details, nonblocking readiness, descriptor passing, and tests that socket ABI is independent of software TCP, local IPC, paravirtual transport, QUIC, or accelerated transport backing. |
| `COMPAT-PERS-001` | Compatibility personality interface | `netbsd_personality_abi.md` defines the first NetBSD-like rump/personality ABI boundary. `userland/netbsd_personality_clang_smoke.c`, `demos/netbsd_rumpfs.img`, and `scripts/run_netbsd_personality_smoke.sh` exercise the real Clang/lld package gate for init-style startup, shell-like command dispatch, `openat(AT_FDCWD, ...)`, pipe/fd inheritance, narrowed FDR descriptor passing over a queue, an mmap-backed allocator arena with `mprotect`/`munmap`, a tiny checked rumpfs mount/read service over a block-image FDR, `poll`/`select`/`epoll`, pthread startup/join, futex wake, `usleep`, `alarm`, timerfd wait, TCP loopback, signal delivery with mask/pending behavior, call-gate delivery, and Resource Domain freeze/resume/attach/detach/destroy with delegated budget isolation; `scripts/run_netbsd_personality_system.sh` adds a namespace-rooted supervisor/init shell transcript, separate executed compiled user programs for cwd/openat, ELF exec, filesystem service, mmap, fd passing, call gates, timers, sockets, signals, divide-by-zero fault-to-`SIGFPE`, nested Resource Domain composition, per-child domains, stale FDR rejection, and Resource Domain PID balance. | Expand the smoke into real NetBSD-derived rump components and add tests for lifecycle events, VMA events, namespace dispatch, richer block-image FDRs, additional signal/fault records, event queues, network endpoints, PCIe BAR/DMA/IRQ-event delegation, and domain-control upcalls, plus negative tests that personalities cannot own raw page tables, raw interrupts, raw DMA, scheduler dispatch, capability minting, or widened transferred capabilities. |
| `COMPAT-CLOUD-001` | Remote attestation surface | Measured boot skeleton is documented. | Add quote FDR tests for bitstream/ROM identity, manifest/image/domain measurements, boot policy, delegated capability-root binding, development quote flag, and malformed quote reads. |
| `COMPAT-CLOUD-002` | Telemetry without privileged scraping | Counters and trace rings are documented. | Add telemetry FDR tests for narrowed aggregate views, per-domain views, redacted tenant-strict views, trace overflow, destructive/snapshot reads, threshold events, and denial without delegated telemetry authority. |
| `COMPAT-CLOUD-003` | Tenant-strict and confidential-domain hooks | Resource Domain security policy exists. | Add tests for mandatory hardening bits, no parent memory inspection without capability, explicit shared pages, scoped DMA, sealed-secret measurement mismatch, and production confidential-mode refusal without memory encryption. |
| `COMPAT-CLOUD-004` | Live-migration compatibility hooks | Checkpoint hooks are documented. | Add tests for service callback events, endpoint drain/redirect hooks, storage barrier integration, dirty tracking, and stale capability reattachment rejection. |
| `COMPAT-ASSURE-001` | Assurance profile levels and policy enforcement split | Assurance profiles are specified. | Add tests for `ASSURANCE_DEV` non-production quote markings, minimum-profile domain admission, field/high/formal profile required bits, PDP requests having no effect until hardware validation, and quote binding of proof/IP/toolchain/build metadata. |
| `COMPAT-ASSURE-002` | Tamper-evident audit stream | Audit stream profile is specified. | Add tests for monotonic sequence numbers, hash-chain roots, dropped-count/gap records, narrowed audit FDR disclosure, quoteable audit roots, destructive/snapshot reads, and proof that audit records cannot encode or revive authority. |
| `COMPAT-ASSURE-003` | Controlled debug and forensics mode | Debug/forensics profile is specified. | Add tests that debug requires debug-control FDR rights per operation, production profiles can lock invasive debug, unlocks are measured/audited, parent-domain inspection is denied without explicit capability, and crash dumps are redacted by tenant/confidential/MLS policy. |
| `COMPAT-ASSURE-004` | Cross-domain and MLS label enforcement | MLS hooks are specified. | Add tests for label generation checks, denied cross-label sends/maps/DMA/debug/telemetry/service replies, explicit audited declassification gates, unlabeled-object rejection/default-label policy, and no bypass through raw interrupts, fault records, trace, parent inspection, or returned capabilities. |
| `COMPAT-ASSURE-005` | Mission assurance profile | Mission profile is specified as a Resource Domain profile. | Add tests for mission profile admission, dependency graph hash checks, dependency-as-FDR enforcement, `normal`/`degraded`/`recovering`/`frozen`/`failed_closed`/`quarantined` transitions, fail-closed dispatch denial, failover without authority broadening, stale service-generation rejection, audit/fault emission, and quoteable mission evidence. |
| `COMPAT-ASSURE-006` | Owner sovereignty and open assurance | Owner-controlled roots and anti-DRM posture are specified. | Add tests for owner-key boot policy, non-production unsigned development policy, quote disclosure of active trust root, reproducible artifact hash fields, owner-held debug-control FDR unlock, no vendor-exclusive key requirement, no hidden management/telemetry/debug/DMA path, and replacement service images preserving capability/domain/lineage checks. |
| `COMPAT-ASSURE-007` | Global progress under bounded faults | The proof roadmap specifies no unspecified stuck state inside the bounded fault model. | Add tests/fault injection for every long command completing, canceling, faulting, timing out, revoking, degrading, or escalating to measured machine-fatal state; parked TIDs having valid wake/cancel/fault sources; watchdog/local reset preserving authority; full queues/fabrics/FIFOs producing documented backpressure; and poisoned metadata never reappearing as fresh authority. |
| `COMPAT-ASSURE-008` | Adversarial input containment | The proof roadmap specifies hostile input containment. Classifier-table adversarial tests cover malformed/oversized records, zero-length IPv4/IPv6 packets, invalid rule descriptors, bad control pointers, invalid profiles, foreign domains, unauthorized destinations, full destination queues, stale/revoked route sources, and fail-before-install behavior. Servicelet verifier tests cover bad envelopes, unsupported/blocking capability bits, excessive program/instruction/cycle/record/action bounds, owner-domain generation checks, and fail-before-install rejection. | Add fuzz/adversarial tests beyond classifier routing and servicelet verification for malformed service replies, hostile peers, runtime servicelet attachment/execution, corrupted file bytes, packet floods, connection floods, service-call storms, revocation races, and timing races; verify outcomes are data, drops, `needs_software`, typed faults, pressure events, quota exhaustion, domain isolation, or measured fatal state, never authority creation, cross-tenant leakage, or unspecified hardware state. |
| `COMPAT-SCHED-001` | Weighted-fair hardware scheduler contract | Basic hardware scheduler behavior exists. | Add tests for hardware thread interleaving without speculative SMT, exactly-one scheduler state/location, fixed weight table charging, virtual runtime/deadline ordering, latency-class placement caps, quota exhaustion/replenishment, hierarchy rollup, ancestor eligibility, affinity mask intersection, wakeup generation matching, bounded wakeup insertion, bounded fairness/approximation, maximum preemption latency, active-window dispatch, DDR spill/refill identity preservation, forced park, stale scheduler-record rejection, call-gate/servicelet charge policy, `ENV_GET` scheduler constants, and no scheduler plugin/callback path. |
| `COMPAT-BOOT-001` | Default operating envelope | Bootable userland image exists over host-directory setup. | Add native boot tests that reset creates root/PID 1 domains, scheduler profile, domain budgets, security defaults, telemetry/fault FDRs, boot/quote records, explicit initial grants, no ambient device authority, and no runnable thread outside a valid domain. |
| `COMPAT-SERVICE-001` | Native service transaction model | Service boundary is specified architecturally. | Implement emulator service-continuation records with request id, service generation, caller domain generation, bounded copied/pinned inputs, capability argument table, returned-capability proposal table, explicit commit point, service crash/freeze behavior, and backpressure outcomes. |
| `COMPAT-STRESS-001` | Capability fuzzing and classed revocation | `randomized_capability_delegation_stress_preserves_authority` provides seeded cap send/dup/seal/revoke stress over narrowed rights, transfer rights, sealed descendants, and revocation invalidation. | Expand to lineage epoch propagation, `lazy_epoch` invalidation, `forced_cancel` wakeups and pre-commit aborts, `synchronous_quiesce` for DMA/page/BAR reuse, `poison_fault` behavior for corrupt metadata, pre/post-commit `EREVOKED` behavior, returned-capability rejection after lineage change, multi-process/channel interleavings, and longer randomized traces. |
| `COMPAT-STRESS-007` | Capability mint/install discipline | Returned-capability verification is documented. | Add tests that namespace, filesystem, PCIe, network, loader, and supervisor service replies are data until committed by the Capability Engine; reject proposals with broadened rights, wrong class, out-of-range BAR/DMA bounds, stale generation, stale lineage, wrong receiver domain, or missing mint/root authority. |
| `COMPAT-STRESS-002` | Domain lifecycle fuzzing | `randomized_domain_lifecycle_stress_rejects_stale_handles` provides seeded create/freeze/resume/destroy stress with generation checks and stale query rejection; `domain_property_invariants_cover_edge_cases` covers domain-tree invariants, stale attach rejection, detach behavior, and parked-thread release across freeze/resume. | Expand to longer concurrent attached-process interleavings, checkpoint query-state and dirty-tracking hooks, and VM/container/cgroup/sandbox profile-equivalence fuzzing. |
| `COMPAT-STRESS-003` | VMA fuzzing | Seeded mmap/munmap/mprotect/guard stress exists. | Expand to page-state-machine transitions, object-backed fill races, stale returned pages, DMA pin/revoke, service timeouts, `PULL`/`PUSH` mapping-coherence profile boundaries, and executable-transition traces. |
| `COMPAT-STRESS-004` | Allocator pressure and hardware heap contract | Multi-threaded malloc/realloc/free pressure test exists. | Expand to randomized mixed-size churn over the LNP64 Default Heap Algorithm, `ALLOC_EX` hint/error behavior, `ALLOC_SIZE` exact-pointer behavior, cross-thread frees, invalid/interior/foreign pointer rejection, quarantine/guard behavior, domain policy denial, memory pressure, hardware-owned allocation object tests, software-owned arena tests, per-thread allocation-window refill/drain behavior, and tests that libc does not depend on hidden freelist/slab/window/quarantine representation. |
| `COMPAT-STRESS-005` | poll/epoll races | Event-before-arm, event-during-blocking-wait, timeout-before-event, descriptor reuse/stale-generation, shared-fd close, multi-source waiter, and repeated pending-churn tests exist for poll/epoll/fd waiters. | Expand to longer randomized scheduler interleavings across poll, select, epoll, close, dup, fork, and pthread fd-table sharing. |
| `COMPAT-STRESS-006` | DMA pending revoke | Emulator v1 has an explicit no-pending-DMA policy and `dma_ctl_v1_completes_before_revoke_and_blocks_later_submits` tests completion-before-revoke plus revoke-before-next-submit. | Reopen when async DMA descriptor queues or completion objects are implemented. |
