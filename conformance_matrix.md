# LNP64 POSIX and Libc Conformance Matrix

This matrix is the compatibility ledger for the emulator, C compiler, and libc
shim surface. It should be updated whenever a real package exposes a missing
runtime, libc, ISA, or ABI behavior.

Status values:

- `implemented`: the API lowers to LNP64 instructions or emulator primitives.
- `tested`: the API has a repository test or scripted real-program smoke test.
- `partial`: useful subset exists, but known POSIX/libc behavior is missing.
- `unsupported`: intentionally outside the current v1 compatibility surface.
- `native extension`: LNP64-specific API or instruction surface.

| API or Scope | Status | Evidence | Known Gaps / Compatibility Bugs |
| --- | --- | --- | --- |
| `_start`, `main(argc, argv, envp)`, `environ` | tested | `c_start_symbol_overrides_main_entry`, `c_main_receives_argc_argv_and_envp_from_startup_page`, `c_main_environ_points_at_startup_envp`, libc-test `argv` | No standalone crt object files yet; startup is compiler/runtime modeled. `COMPAT-ABI-001` tracks psABI/crt packaging. |
| `getauxval`, auxv metadata, process entry metadata | tested | `c_getauxval_startup_metadata_surface_runs`, `ENV_GET` emulator tests | Dynamic loader auxv contract is not frozen. Covered by `COMPAT-ABI-002`. |
| TLS/thread pointer, `errno`, `strerror` | tested | `c_thread_pointer_and_specific_storage_are_per_thread`, `c_errno_location_shim_tracks_hardware_errno`, `c_strerror_returns_static_errno_messages`, `psABI.md` | Dynamic TLS module layout is not frozen. |
| `atexit`, `exit`, `_exit` | tested | `c_atexit_handlers_run_before_main_return`, `c_exit_runs_atexit_but_exit_bypasses_it` | No shared-object destructor model. |
| Environment APIs: `getenv`, `setenv`, `unsetenv`, `clearenv`, `putenv` | tested | `c_environment_surface_stores_and_finds_values`, libc-test `env` | Locale/environment inheritance across `exec` needs more real-program coverage; the mutable `environ` table is compact and intentionally small. |
| Basic file descriptors: `open`, `openat`-style lowering, `close`, `read`, `write`, `lseek` | tested | `lowers_file_builtins_to_fd_instructions`, `scripts/run_sbase.sh` | Host-backed file model, not a boot image VFS yet. |
| Positioned and vectored I/O: `pread`, `pwrite`, `readv`, `writev` | tested | `c_readv_writev_surface_uses_dynamic_fdr_io`, emulator fd dispatch tests | Scatter/gather edge cases need real package coverage. |
| Descriptor duplication: `dup`, `dup2`, `fcntl`-style capability duplication | tested | `c_posix_descriptor_dup_surface_runs_on_cap_dup`, `c_fcntl_unsupported_constant_fails_with_einval`, `c_fcntl_accepts_dynamic_command_expressions`, dynamic FDR token tests, libc-test `fcntl` | Full Linux flag and lock edge cases are partial. `COMPAT-LIBC-001`. |
| Metadata and filesystem mutation: `stat`, `fstat`, `chmod`, `chown`, `link`, `symlink`, `mkdir`, `unlink`, `rename`, `utime`/`touch` | partial | `scripts/run_sbase.sh` covers `chmod`, `chown`, `ln`, `mkdir`, `rm`, `mv`, `touch`, `find`, `ls`; libc-test `stat`/`utime` | Symlink/path corner cases, permissions, hard-link errors, and full `stat` fields need conformance tests. `COMPAT-FS-001`. |
| Directory iteration: `opendir`, `readdir`, `rewinddir`, `closedir` | tested | `c_directory_iteration_surface_reads_entries`, `scripts/run_sbase.sh` (`ls`, `find`) | Concurrent directory mutation semantics are not specified. |
| stdio streams: `fopen`, `freopen`, `fread`, `fwrite`, `fgets`, `fprintf`, `fscanf` scanset subset, `ungetc`, `feof`, `fflush`, `fclose`, `fseek`, `ftell`, `tmpfile`, `setvbuf` | tested | `c_fgets_reads_lines_from_descriptor_stream`, `c_fprintf_writes_formatted_output_to_descriptor_stream`, `c_ungetc_scanset_and_fread_pushback_run`, `c_stdio_*`, `c_tmpfile_returns_read_write_unlinked_stream`, `c_freopen_replaces_descriptor_stream`, libc-test `fdopen`/`ungetc`/`fgets-eof` | General `scanf` grammar, buffered stream error flags, and wide I/O are partial. `COMPAT-STDIO-001`. |
| Formatting and string/memory helpers: `printf` subset, `snprintf` subset, `str*`, `mem*`, integer division, `ctype` | partial | Lua portability/string tests, `c_standard_ctype_surface_runs`, `indirect_memcpy_and_memset_builtins_have_callable_labels`, sbase/jsmn scripts, libc-test `string`/`string_memmem`/`string_memmove_bounded`/`string_strchr`/`string_strcspn`/`string_strstr`/`udiv` | Full format grammar, floating formatting, locale collation, and wide chars are partial. `COMPAT-LIBC-002`. |
| Math/libm used by real packages | partial | `c_libm_integer_model_shims_run` | Integer-model shims only; real floating libm remains incomplete. `COMPAT-LIBM-001`. |
| Allocation: `malloc`, `free`, `realloc`, `calloc`, `aligned_alloc`, `posix_memalign` | tested | `c_allocator_libc_surface_uses_native_heap_metadata`, `c_multithreaded_allocator_pressure_runs`, allocator demos, libc-test `regression/malloc-0` | `malloc(0)` returns non-null unique pointers. Multi-threaded allocator pressure exists; longer randomized mixed-size churn remains under `COMPAT-STRESS-004`. |
| `brk`, `sbrk` | tested | `c_brk_sbrk_compat_surface_uses_native_heap` | Compatibility cursor is modeled over native allocations, not a contiguous Unix heap. |
| `mmap`, `munmap`, `mprotect` | tested | `c_mmap_mprotect_and_munmap_surface_runs`, VMA security tests | Add Object-Backed Page Transaction Protocol tests. File-backed shared mapping coherence is service/personality policy. `COMPAT-MM-001`. |
| W^X, NX, guard pages, ASLR, `RANDOM` | tested | `wx_mmap_and_mprotect_follow_domain_policy`, `nx_and_guard_instruction_fetches_fault`, `heap_and_anonymous_mmap_use_aslr_layout`, `random_scalar_and_buffer_are_deterministic`, `randomized_mmap_mprotect_and_guard_stress_preserves_permissions` | Seeded VMA/guard stress exists; broader file/shared mapping fuzzing remains under `COMPAT-STRESS-003`. |
| `getentropy`, `getrandom`, `arc4random`, `arc4random_buf` | tested | `c_entropy_surface_lowers_to_random_instruction`, `random_obeys_domain_entropy_quota` | Cryptographic entropy source is intentionally not modeled; deterministic emulator RNG is for architecture behavior. |
| `pthread_create`, `pthread_join`, `pthread_self`, thread-specific storage | tested | `c_thread_pointer_and_specific_storage_are_per_thread`, `c_pthread_tsd_destructors_run_on_thread_return`, libc-test `pthread_tsd`, pthread tests | `pthread_detach`, cancellation, robust mutexes, and scheduler fairness are partial. `COMPAT-PTHREAD-001`. |
| Mutexes, condvars, rwlocks, `pthread_once`, semaphores | tested | `c_pthread_mutex_condvar_surface_runs_on_futex_primitives`, `c_semaphore_and_once_surface_runs_on_futex_primitives`, `c_posix_semaphore_timedwait_and_returning_pthread_start_run`, `c_rwlock_surface_runs_on_futex_primitives`, libc-test `sem_init` | Timed semaphore waits are covered; broader timed condvar/rwlock and process-shared behavior remain partial. `COMPAT-PTHREAD-002`. |
| C11 atomics and futex-backed waits | tested | `c_c11_atomic_surface_runs_on_lock_cmpxchg`, emulator futex tests | Add TSO/seq-cst/futex acquire-release litmus tests under `COMPAT-MEM-001`. |
| `poll`, `select`, `epoll_create1`, `epoll_ctl`, `epoll_wait` | tested | `c_select_fdset_surface_lowers_to_readiness_probe_and_runs`, `c_select_blocks_with_dynamic_await_and_runs`, `c_poll_*`, `c_epoll_surface_lowers_to_native_readiness_probe_and_runs`, `c_poll_race_timing_cases_run`, `c_epoll_wait_race_timing_cases_run`, `c_poll_closed_shared_descriptor_reports_pollnval`, `stale_fd_waiter_rejects_reused_event_source_generation`, `stale_dynamic_fd_waiter_reports_error_result`, `multi_source_fd_waiters_wake_only_ready_sources`, `fd_waiters_survive_repeated_pending_churn_until_ready` | Basic race timing, descriptor reuse/stale-generation, shared-fd close, multi-source waiter, and repeated pending-churn coverage exists; broader randomized scheduler interleavings remain under `COMPAT-STRESS-005`. |
| `eventfd`, `timerfd_create`, `timerfd_settime`, `timerfd_gettime` | tested | `c_eventfd_surface_uses_counter_object_profile`, `c_timerfd_surface_uses_object_timer_profile` | Full Linux flag and clock-id behavior is partial. |
| `clock_gettime`, `gettimeofday`, `time`, `nanosleep`, `usleep`, `alarm` | tested | `c_time_surface_uses_realtime_pcrs_and_sleep`, `c_usleep_and_alarm_surface_runs`, libc-test `clock_gettime` | Time precision is emulator-coarse. |
| `fork`, `exec*`, `wait`, `waitpid`, `getpid`, `getppid` | tested | `c_posix_process_and_signal_mask_surface_runs`, `c_wait_and_getppid_surface_runs_after_fork`, `c_exec_family_lowers_to_native_exec`, emulator clone/exec tests | `fork` is constrained `CLONE profile=posix_fork`; add stress for FDR inheritance, signal-mask/pending behavior, and no in-flight ownership copying. `exec` commits emulator-native images today; software binary-loader work is tracked as `COMPAT-BIN-001`. |
| Frozen signal subset: `sigaction`, `signal`, `sigprocmask`, `raise`, `kill`, `alarm`, default/ignore/handler dispositions, signal frames, `SIGRET` | tested | `c_sigaction_accepts_posix_action_struct`, `c_signal_default_and_ignore_dispositions_run`, `signal_frame_stack_area_is_non_executable` | Add stress coverage for pending/mask/nesting boundaries. Full realtime queueing, legacy restart quirks, and OS-specific delivery corners remain compatibility policy. `COMPAT-SIGNAL-001`. |
| Sockets over endpoint capabilities: `socket`, `bind`, `listen`, `connect`, `accept`, `getsockname`, `getsockopt`, `setsockopt`, `send`, `recv` | tested | `c_socket_surface_lowers_to_endpoint_object_controls_and_runs`, `demos/netcat.c`, `demos/httpd.c` in `scripts/run_demos.sh` | Endpoint capability shape is native; TCP/UDP semantics are service-owned. Nonblocking sockets, descriptor passing, datagram endpoints, and real network-driver service integration are partial. `COMPAT-SOCK-001`. |
| Optional dynamic loading and subprocess streams: `dlopen`, `dlsym`, `dlclose`, `popen`, `pclose` | unsupported | `c_optional_dynamic_loading_and_popen_fail_cleanly` | Intentionally fail cleanly until binary/dynamic-loader work exists. |
| Locale, wide chars, iconv, regex | partial | sbase text utilities exercise simple byte-string behavior | Full locale/wide/regex conformance is not implemented. `COMPAT-LIBC-003`. |
| LNP64 `__lnp_*` shim layer | native extension | `c_private_lnp_shim_layer_lowers_to_native_primitives`, `c_private_lnp_shim_layer_accepts_dynamic_fdr_tokens`, `psABI.md` | Public extension header/package boundary is not frozen. |
| Resource Domain APIs | native extension | `c_domain_lifecycle_surface_runs_on_domain_ctl`, `c_domain_limit_failure_runs`, domain emulator tests, `randomized_domain_lifecycle_stress_rejects_stale_handles` | VM/container/cgroup/sandbox profiles should all lower to the same child-domain primitive. Seeded lifecycle stress exists; nested-domain and attached-process interleavings remain under `COMPAT-STRESS-002`. |
| Capability APIs: send, receive, duplicate, narrow, seal, revoke, generation/lineage checks | native extension | `c_capability_transfer_surface_runs_on_native_cap_ops`, capability emulator tests, `randomized_capability_delegation_stress_preserves_authority` | Seeded delegation stress exists; add lineage epoch propagation across FDR caches, VMAs, events, call gates, DMA, and page-fill continuations. Multi-process/channel interleavings remain under `COMPAT-STRESS-001`. |
| Object APIs: queue/counter/memory object, pipe lowering, message receive | native extension | `c_object_creation_surface_runs_on_object_ctl`, `c_pipe_lowers_to_object_queue_and_runs`, `c_message_receive_lowers_to_await_pull_and_runs` | Multi-producer/multi-consumer race coverage is partial. |
| Typed control envelope: `GET_META`, `SET_META`, `OBJECT_CTL`, `DOMAIN_CTL`, `NS_CTL` | native extension | architecture docs | Add malformed envelope, unknown version/profile class/object/profile/op, unsupported flags, missing rights, stale lineage, bounded length, scalar count, capability argument table, returned-capability slot, payload-as-data, vendor/device profile authority, copied vs pinned buffer behavior, `EFAULT`/`EOVERFLOW`/`EBUSY`/`ECANCELED`, revoke during control, service crash before/after commit, commit point, and returned-capability verification tests. |
| Native service transaction model | specified | `design.md`, `hardware_design.md`, `formal_theorems.md` | Add tests for bounded request records, service generation mismatch, service crash before commit, service crash after commit, returned-capability proposals as data, Capability Engine install rejection, queue backpressure wait/`EAGAIN`/`EOVERFLOW`, revocation during pending service work, pinned-buffer cancellation, and no ambient raw pointer/interrupt/DMA capability exposure. `COMPAT-SERVICE-001`. |
| Call gates: sync, async, handoff | native extension | `c_sync_call_gate_runs`, `call_cap_sync_returns_across_domain_gate`, `call_cap_async_and_handoff_modes_execute_minimally`, `psABI.md` | Full cross-domain aggregate argument ABI is not frozen. |
| DMA APIs and DMA buffers | native extension | `dma_ctl_copy_and_fill_use_vma_permissions`, `dma_ctl_rejects_guard_unmapped_and_disallowed_domain`, `dma_ctl_uses_dma_buffer_capability_scope`, `dma_ctl_rejects_stale_and_revoked_dma_buffers`, `dma_ctl_v1_completes_before_revoke_and_blocks_later_submits` | Emulator v1 is synchronous copy/fill only, with no pending descriptor queue. Async DMA completion and quiesce/cancel policy remain future hardware work. |
| Static ELF objects, static PIE, relocations, exec-plan loader boundary | partial | `object_format.md`, `psABI.md` | Static v1 software-loader ELF profile is specified; loader-produced exec plans and relocation implementation remain `COMPAT-BIN-001`. This is a software-loader/toolchain contract; hardware validates only the resulting exec-plan descriptor and preserves the old image on pre-commit rejection. |
| Dynamic linker and shared-library runtime | unsupported | `object_format.md`, `psABI.md`, `c_optional_dynamic_loading_and_popen_fail_cleanly` | Intentionally fail cleanly in v1. Future dynamic loading is a software personality/loader profile, not a hardware feature. |
| Bootable userland image: `/sbin/init`, shell-like runner, filesystem image, `/dev` namespace | tested | `userland/init.c`, `userland/lnpsh.c`, `scripts/run_userland.sh` | Minimal host-directory image boots and inspects itself; native boot manifest/VFS integration remains under `COMPAT-USERLAND-001`. |

## Real Program Targets

These targets are the current compatibility gates. A target only moves to
`passing` when it builds and runs through a checked-in script without package-
specific compiler/runtime special casing.

| Target | Status | Evidence | Compatibility Bugs |
| --- | --- | --- | --- |
| sbase subset | passing | `third_party/sbase/*`, `scripts/run_sbase.sh` | Expand command coverage and edge cases under `COMPAT-FS-001`, `COMPAT-STDIO-001`, `COMPAT-LIBC-002`. |
| jsmn | passing | `third_party/jsmn/example/simple.c`, `third_party/jsmn/test/tests.c`, `scripts/run_jsmn.sh` | None known beyond broader C parser/runtime coverage. |
| inih | passing | `third_party/inih/ini.c`, `third_party/inih/smoke.c`, `scripts/run_inih.sh` | File parser and `ini_parse_string` paths pass. Broader allocator/custom-reader configurations are not covered. |
| zlib upstream | partial / passing checksums | `third_party/zlib/adler32.c`, `third_party/zlib/crc32.c`, `third_party/zlib/crc32.h`, `third_party/zlib/zutil.c`, `third_party/zlib/smoke.c`, `scripts/run_zlib.sh` | Full deflate/inflate remain `COMPAT-PKG-002`. |
| natsort | passing | `third_party/natsort/strnatcmp.c`, `third_party/natsort/smoke.c`, `scripts/run_natsort.sh` | None known beyond broader C parser/runtime coverage. |
| cwalk | passing | `third_party/cwalk/include/cwalk.h`, `third_party/cwalk/src/cwalk.c`, `third_party/cwalk/smoke.c`, `scripts/run_cwalk.sh` | Unix and Windows style selection, normalize, join, join-multiple, absolute/relative generation, intersection, basename/dirname, segment iteration, and extension replacement pass. Broader edge-case enumeration remains under `COMPAT-PKG-007`. |
| small HTTP server | passing | `demos/httpd.c`, `scripts/run_demos.sh` | Socket nonblocking and network-service semantics tracked by `COMPAT-SOCK-001`. |
| netcat-like socket demo | passing | `demos/netcat.c`, `scripts/run_demos.sh` | Socket nonblocking and descriptor passing tracked by `COMPAT-SOCK-001`. |
| sqlite-lite demo | passing | `demos/sqlite_lite.c`, `demos/sqlite_lite.db`, `scripts/run_demos.sh` | This is not upstream SQLite; full SQLite remains `COMPAT-PKG-003`. |
| minimal userland image | passing | `userland/init.c`, `userland/lnpsh.c`, `scripts/run_userland.sh` | Native boot manifest/VFS integration remains `COMPAT-USERLAND-001`. |
| Lua upstream | failing / not checked in | Lua-targeted compiler tests exist; no checked-in full Lua package gate | Remove Lua-specific normalizer pressure by fixing generic C semantics. `COMPAT-PKG-001`. |
| SQLite upstream | not started | No checked-in SQLite target | Add package gate. `COMPAT-PKG-003`. |
| libpng upstream | not started | No checked-in libpng target | Add package gate after zlib. `COMPAT-PKG-004`. |
| musl libc-test subset | partial / passing focused checks | `third_party/libc-test/functional/{argv.c,basename.c,clock_gettime.c,dirname.c,env.c,fcntl.c,fdopen.c,pthread_tsd.c,qsort_bounded.c,random.c,search_insque.c,search_lsearch.c,sem_init.c,stat.c,string.c,string_memmem.c,string_strchr.c,string_strcspn.c,string_strstr.c,strtol.c,udiv.c,ungetc.c,utime.c}`, `third_party/libc-test/regression/{fgets-eof.c,malloc-0.c}`, `scripts/run_libc_test.sh` | Expand beyond the initial path/string subset. `COMPAT-PKG-005` tracks harness and libc gaps. |

## Open Compatibility Bugs

| Bug | Requirement | Current State | Next Concrete Step |
| --- | --- | --- | --- |
| `COMPAT-ABI-001` | psABI/crt packaging | `psABI.md` records the current emulator ABI; startup is compiler/runtime modeled. | Decide whether v1 ships crt objects or keeps compiler-emitted startup as the contract. |
| `COMPAT-ABI-002` | auxv/static-loader metadata contract | `psABI.md` records current auxv behavior; `ENV_GET` and `getauxval` exist. | Freeze auxv key numbers and static loader metadata. Dynamic-loader expectations belong to a future software profile. |
| `COMPAT-BIN-001` | Binary/object format | `object_format.md` defines static v1 software-loader ELF, relocations, mapping permissions, ASLR, static-only dynamic-linking policy, startup descriptors, and the hardware exec-plan boundary. | Implement a software ELF loader that produces exec-plan descriptors plus relocation tests, descriptor-shape rejection tests, W^X/NX/provenance rejection tests, FDR grant validation tests, generation/lineage mismatch tests, and old-image-survives-pre-commit-failure tests. |
| `COMPAT-USERLAND-001` | Minimal userland image | Host-directory image with `/sbin/init.s`, `/bin/lnpsh.s`, `/etc`, `/dev`, and `/tmp` passes `scripts/run_userland.sh`. | Add native boot manifest/service-image format and boot command instead of host-directory setup. |
| `COMPAT-PKG-001` | Upstream Lua | Lua compatibility is covered by targeted compiler tests only. | Add a reproducible upstream Lua package script and convert failures into generic compiler/runtime bugs. |
| `COMPAT-PKG-002` | zlib | Upstream v1.3.1 Adler-32 and CRC-32 checksum paths pass through `scripts/run_zlib.sh`. | Expand to deflate/inflate once struct-heavy stream state and broader compression runtime paths are ready. |
| `COMPAT-PKG-003` | Upstream SQLite | Only sqlite-lite demo exists. | Add upstream amalgamation smoke once C/runtime gaps are known. |
| `COMPAT-PKG-004` | libpng | Not started. | Add after zlib passes. |
| `COMPAT-PKG-005` | musl libc-test subset | Upstream libc-test `argv`, `basename`, `clock_gettime`, `dirname`, `env`, `fcntl`, `fdopen`, `pthread_tsd`, `random`, `search_insque`, `search_lsearch`, `sem_init`, `stat`, `string`, `string_memmem`, `string_strchr`, `string_strcspn`, `string_strstr`, `strtol`, `udiv`, `ungetc`, and `utime`, plus upstream regressions `fgets-eof` and `malloc-0`, bounded qsort coverage, and local bounded `string_memmove_bounded` coverage pass with a minimal LNP64 harness. Probed blockers include `basename`/`dirname` now-fixed null/all-slash behavior, high-byte C string escapes now-fixed by byte-exact string emission, file-scope static byte arrays now-fixed for `string_strchr`, file-scope two-dimensional byte arrays now covered by `search_lsearch`, word-pointer parameter indexing and POSIX `random`/`srandom`/`initstate`/`setstate` state semantics now covered by `random`, intrusive queue `insque`/`remque` and inferred queue-node sizing now covered by `search_insque`, descriptor-backed `fdopen` stream reuse now covered by `fdopen`, mutable `environ` now covered by `env`, variadic macros now covered by `argv`, thread-specific storage destructors and `pthread_key_t` aliases now covered by `pthread_tsd`, `memmem` byte-search semantics now covered by `string_memmem`, bounded direct/callable `memmove` overlap semantics now covered by `string_memmove_bounded`, `lfind`/`lsearch` fixed-width search semantics now covered by `search_lsearch`, bounded `strncpy`/`strncat`, `strtok`, and `strlcat` return semantics now covered by `string`, unnamed semaphore init/wait/post, try/timed wait errno behavior, and pthread start routines that return normally now covered by `sem_init`, non-null unique zero-size allocations now covered by `malloc-0`, anonymous static aggregate arrays now covered by `udiv`, base/sign `strto*` parsing, `ERANGE`/`EINVAL`, octal C integer literals, qsort comparator word-cast loads, pointer-to-pointer parameter indexing, global-array `sizeof`, qsort string/int/char/uint64 paths, descriptor-backed `ungetc` pushback/scanset mismatch/fread consumption, descriptor duplication/flags behavior through `fcntl`, stat metadata, timestamp updates, and EOF behavior for `fgets` now covered. Full upstream `string_memcpy`/`string_memset` exhaustive alignment loops and full upstream qsort's 1024-element cases remain too slow for the default gate; broader scanf/regex/wide/dynamic-linking tests remain open. | Add more bounded memory-function gates or optimize emulator throughput for the full upstream memory/qsort stress tests; next practical targets include additional small scalar/time/path libc-test files. |
| `COMPAT-PKG-006` | inih parse-string aggregate path | File parser and `ini_parse_string` smoke paths pass with upstream `inih` r58. The gate fixed generic typedef-aggregate alias layout and function-scoped aggregate-local metadata. | Expand with heap-buffer/realloc/custom-reader configurations if those `inih` build modes become targets. |
| `COMPAT-PKG-007` | cwalk path library | Upstream cwalk commit `e98d23f` has a checked smoke gate for Unix and Windows style selection, absolute/relative predicates, basename/dirname output parameters, segment iteration and replacement, extension detection/replacement corner cases, normalization, path joining, absolute and relative path generation, intersection detection, truncation lengths, empty/root-only paths, repeated separators, mixed separator normalization, drive-relative Windows roots, UNC root variants, relative backtracking, and in-place output overlap. The gate fixed generic C/frontend/runtime gaps for `do ... while` `continue` semantics, word-slot indexing through member pointer arrays, and overlap-safe callable `memmove`. | Broaden further only by importing more of upstream's own test matrix or adding randomized path-case generation. |
| `COMPAT-FS-001` | Filesystem/path conformance | sbase covers common commands. | Add negative/error path tests for symlinks, permissions, `stat`, and rename/link corner cases. |
| `COMPAT-STDIO-001` | stdio conformance | Common descriptor-backed streams pass; `ungetc` one-byte pushback, `feof` false-before-EOF, `fscanf` scanset mismatch preservation, and `fread` consumption of ungot bytes are covered. | Add real EOF/error flag transitions, general `scanf`, buffering, append, and mixed read/write tests. |
| `COMPAT-LIBC-001` | Full `fcntl` command surface | Descriptor duplication, dynamic command dispatch for the supported subset, advisory locks, and unsupported-command `EINVAL` behavior are tested. | Add more Linux flag and lock edge cases. |
| `COMPAT-LIBC-002` | Formatting/string/locale | Byte-string subset passes real demos. | Expand printf/scanf, locale, and collation tests. |
| `COMPAT-LIBC-003` | Wide char/regex/iconv | Mostly unsupported. | Decide v1 boundary and add fail-cleanly tests. |
| `COMPAT-LIBM-001` | Floating libm | Integer-model shims exist. | Add real double representation or document static integer-only v1 limit. |
| `COMPAT-MM-001` | VMA/Page Engine state machine and object-backed mmap/fill transactions | Anonymous mapping security is well tested. | Add page-state transition tests, commit/abort point tests, deterministic race-priority tests, page request/reply shape tests, object-fill success/failure, zero/shared-page replies, retry/later behavior, VMA/object generation mismatch rejection, lineage mismatch rejection, permission/memory-type narrowing, executable provenance checks, DMA pin/revoke interactions, TLB/I-cache invalidation ordering, pending-fill cancellation on munmap/mprotect/revoke/truncate/signal/domain teardown, dirty-range enumeration, service-owned `msync`, and shared/file mapping smoke tests. |
| `COMPAT-MEM-001` | Memory visibility contract | Basic atomics/futex behavior exists. | Add litmus tests for TSO store ordering, store-buffer forwarding, seq-cst `LOCK_CMPXCHG`, futex acquire/release wait/wake, `FENCE` ordering for DMA/engine completions, write-combining fence-before-doorbell, `device_ordered` MMIO ordering, and TLB/I-cache invalidation before resumed execution. |
| `COMPAT-PTHREAD-001` | Full pthread lifecycle | Core create/join/TLS works. | Add detach, cancellation boundary, and join error tests. |
| `COMPAT-PTHREAD-002` | Timed/process-shared sync | Futex-backed primitives work. | Add timed waits and unsupported process-shared behavior tests. |
| `COMPAT-SIGNAL-001` | Frozen signal subset | Default/ignore/action/mask subset works. | Add process-vs-thread pending delivery, deterministic process-directed target selection, checked `kill`, `raise`, `alarm`, synchronous fault-to-signal mapping, trusted `SIGRET` token/generation, nested-delivery boundary, interruptible operation `EINTR`/typed-interrupt behavior, fatal default behavior, signal frame NX/guard checks, and unsupported realtime/restart/altstack fail-or-emulate tests. |
| `COMPAT-SOCK-001` | Socket endpoint compatibility | TCP-like local endpoint subset works over endpoint objects. | Add typed endpoint-contract tests: packet queue record boundaries, datagram message boundaries, stream ordered-byte/backpressure behavior, listener accepted-capability inheritance, nonblocking readiness, descriptor passing, socket-option fail-closed behavior, and tests that socket ABI is independent of software TCP, local IPC, paravirtual transport, QUIC, or accelerated transport backing. |
| `COMPAT-PERS-001` | Compatibility personality interface | Architecture docs define paravirtual Linux, Linux syscall runtime, and NetBSD rump-kernel style approaches. | Add tests/demos for fixed personality surfaces: lifecycle events, VMA events, FDR/capability transfer, namespace dispatch, block-image FDRs, signal/fault records, event queues, network endpoints, PCIe BAR/DMA/IRQ-event delegation, and domain-control upcalls, plus negative tests that personalities cannot own raw page tables, raw interrupts, raw DMA, scheduler dispatch, or capability minting. |
| `COMPAT-CLOUD-001` | Remote attestation surface | Measured boot skeleton is documented. | Add quote FDR tests for bitstream/ROM identity, manifest/image/domain measurements, boot policy, delegated capability-root binding, development quote flag, and malformed quote reads. |
| `COMPAT-CLOUD-002` | Telemetry without privileged scraping | Counters and trace rings are documented. | Add telemetry FDR tests for narrowed aggregate views, per-domain views, redacted tenant-strict views, trace overflow, destructive/snapshot reads, threshold events, and denial without delegated telemetry authority. |
| `COMPAT-CLOUD-003` | Tenant-strict and confidential-domain hooks | Resource Domain security policy exists. | Add tests for mandatory hardening bits, no parent memory inspection without capability, explicit shared pages, scoped DMA, sealed-secret measurement mismatch, and production confidential-mode refusal without memory encryption. |
| `COMPAT-CLOUD-004` | Live-migration compatibility hooks | Checkpoint hooks are documented. | Add tests for service callback events, endpoint drain/redirect hooks, storage barrier integration, dirty tracking, and stale capability reattachment rejection. |
| `COMPAT-ASSURE-001` | Assurance profile levels and policy enforcement split | Assurance profiles are specified. | Add tests for `ASSURANCE_DEV` non-production quote markings, minimum-profile domain admission, field/high/formal profile required bits, PDP requests having no effect until hardware validation, and quote binding of proof/IP/toolchain/build metadata. |
| `COMPAT-ASSURE-002` | Tamper-evident audit stream | Audit stream profile is specified. | Add tests for monotonic sequence numbers, hash-chain roots, dropped-count/gap records, narrowed audit FDR disclosure, quoteable audit roots, destructive/snapshot reads, and proof that audit records cannot encode or revive authority. |
| `COMPAT-ASSURE-003` | Controlled debug and forensics mode | Debug/forensics profile is specified. | Add tests that debug requires debug-control FDR rights per operation, production profiles can lock invasive debug, unlocks are measured/audited, parent-domain inspection is denied without explicit capability, and crash dumps are redacted by tenant/confidential/MLS policy. |
| `COMPAT-ASSURE-004` | Cross-domain and MLS label enforcement | MLS hooks are specified. | Add tests for label generation checks, denied cross-label sends/maps/DMA/debug/telemetry/service replies, explicit audited declassification gates, unlabeled-object rejection/default-label policy, and no bypass through raw interrupts, fault records, trace, parent inspection, or returned capabilities. |
| `COMPAT-ASSURE-005` | Mission assurance profile | Mission profile is specified as a Resource Domain profile. | Add tests for mission profile admission, dependency graph hash checks, dependency-as-FDR enforcement, `normal`/`degraded`/`recovering`/`frozen`/`failed_closed`/`quarantined` transitions, fail-closed dispatch denial, failover without authority broadening, stale service-generation rejection, audit/fault emission, and quoteable mission evidence. |
| `COMPAT-ASSURE-006` | Owner sovereignty and open assurance | Owner-controlled roots and anti-DRM posture are specified. | Add tests for owner-key boot policy, non-production unsigned development policy, quote disclosure of active trust root, reproducible artifact hash fields, owner-held debug-control FDR unlock, no vendor-exclusive key requirement, no hidden management/telemetry/debug/DMA path, and replacement service images preserving capability/domain/lineage checks. |
| `COMPAT-SCHED-001` | Weighted-fair hardware scheduler contract | Basic hardware scheduler behavior exists. | Add tests for fixed weight table charging, virtual runtime/deadline ordering, quota exhaustion/replenishment, hierarchy rollup, wakeup insertion, bounded fairness/approximation, call-gate charge policy, affinity masks, and no scheduler plugin/callback path. |
| `COMPAT-BOOT-001` | Default operating envelope | Bootable userland image exists over host-directory setup. | Add native boot tests that reset creates root/PID 1 domains, scheduler profile, domain budgets, security defaults, telemetry/fault FDRs, boot/quote records, explicit initial grants, no ambient device authority, and no runnable thread outside a valid domain. |
| `COMPAT-SERVICE-001` | Native service transaction model | Service boundary is specified architecturally. | Implement emulator service-continuation records with request id, service generation, caller domain generation, bounded copied/pinned inputs, capability argument table, returned-capability proposal table, explicit commit point, service crash/freeze behavior, and backpressure outcomes. |
| `COMPAT-STRESS-001` | Capability fuzzing and classed revocation | Seeded cap send/dup/seal/revoke stress exists. | Expand to lineage epoch propagation, `lazy_epoch` invalidation, `forced_cancel` wakeups and pre-commit aborts, `synchronous_quiesce` for DMA/page/BAR reuse, `poison_fault` behavior for corrupt metadata, pre/post-commit `EREVOKED` behavior, returned-capability rejection after lineage change, multi-process/channel interleavings, and longer randomized traces. |
| `COMPAT-STRESS-007` | Capability mint/install discipline | Returned-capability verification is documented. | Add tests that namespace, filesystem, PCIe, network, loader, and supervisor service replies are data until committed by the Capability Engine; reject proposals with broadened rights, wrong class, out-of-range BAR/DMA bounds, stale generation, stale lineage, wrong receiver domain, or missing mint/root authority. |
| `COMPAT-STRESS-002` | Domain lifecycle fuzzing | Seeded create/freeze/resume/destroy stress exists. | Expand to VM/container/cgroup/sandbox profile equivalence, nested domains, attached processes, checkpoint query-state and dirty-tracking hooks, explicit reattachment rejection for stale capabilities, and concurrent parked-thread release cases. |
| `COMPAT-STRESS-003` | VMA fuzzing | Seeded mmap/munmap/mprotect/guard stress exists. | Expand to page-state-machine transitions, object-backed fill races, stale returned pages, DMA pin/revoke, service timeouts, `PULL`/`PUSH` mapping-coherence profile boundaries, and executable-transition traces. |
| `COMPAT-STRESS-004` | Allocator pressure and hardware heap contract | Multi-threaded malloc/realloc/free pressure test exists. | Expand to randomized mixed-size churn over the LNP64 Default Heap Algorithm, `ALLOC_EX` hint/error behavior, `ALLOC_SIZE` exact-pointer behavior, cross-thread frees, invalid/interior/foreign pointer rejection, quarantine/guard behavior, domain policy denial, memory pressure, hardware-owned allocation object tests, software-owned arena tests, per-thread allocation-window refill/drain behavior, and tests that libc does not depend on hidden freelist/slab/window/quarantine representation. |
| `COMPAT-STRESS-005` | poll/epoll races | Event-before-arm, event-during-blocking-wait, timeout-before-event, descriptor reuse/stale-generation, shared-fd close, multi-source waiter, and repeated pending-churn tests exist for poll/epoll/fd waiters. | Expand to longer randomized scheduler interleavings across poll, select, epoll, close, dup, fork, and pthread fd-table sharing. |
| `COMPAT-STRESS-006` | DMA pending revoke | Emulator v1 has an explicit no-pending-DMA policy and `dma_ctl_v1_completes_before_revoke_and_blocks_later_submits` tests completion-before-revoke plus revoke-before-next-submit. | Reopen when async DMA descriptor queues or completion objects are implemented. |
